# CONSTELLATION — 引き継ぎメモ

美術鑑賞記録アプリ。展覧会で見た作品とキャプション情報を、時間軸に沿って星座のようにキャンバス上へ記録していく個人用ツール。

---

## コアコンセプト

- **主星(しゅせい)**:作品の写真・動画
- **伴星(ばんせい)**:キャプション・資料などのテキスト情報(OCRまたは手入力)
- 主星と伴星は「記録した時刻が近ければ近くに置く」という緩いルールでペアリングされる(GPSなどの厳密な位置情報は使わない)
- 帰宅後、EXIFタイムスタンプを手がかりに、現地で撮った高画質写真を仮画像と自動的に差し替える運用も想定

## 絶対条件

- **完全無料運用**。Gemini APIキーには請求先アカウントを絶対に紐付けない(無料枠超過時は課金でなくエラーになるようにする)
- 個人利用。専業エンジニアではないユーザーが、Claude(このチャット)とClaude Codeの助けを借りて構築している
- **アプリ側からDrive上の元画像・元ファイルを削除・変更する機能は一切作らない(2026年9月、致命的バグを受けて確定した方針)**。Driveはこのアプリにとって唯一のバックアップであり、アプリ側から書き戻す・消すという発想自体を持たない。経緯・詳細は「Extractの致命的なバグ」セクション参照

---

## Extractの致命的なバグ: 実機でDrive上の元画像を誤って完全削除(2026年9月、修正済み)

写真カードの編集ガイド「Extract」でOCR抽出したテキストをどう使うか確認するダイアログが
`window.confirm()`の「OK」「キャンセル」だったため、**「OK」がどちらの意味か分かりにくく、
ユーザーが実際の展覧会場での操作中に誤って「元画像を破棄してテクストカードに変換」の方を選んで
しまい、二度と戻せない写真をDrive上から完全に失う**という実機事故が起きた。

- **わかりにくさの正体**: `js/app.js`には既に`showChoiceDialog()`という「両方とも意味のある
  選択肢が並ぶ二択」専用のダイアログ(ラベル付きの対等なボタン、`handleCreateSession()`の
  「OCRで読み取る/手入力する」等で使用済み)があり、この設計判断自体はユーザーが以前から
  明確に指示していたものだった(過去のセッションでメモにも残されていた)。**にもかかわらず
  Extractの確認だけはこの置き換えが漏れており、`window.confirm()`のままだった**。1箇所でも
  漏れると事故に直結する典型例
- **もう一段深刻だった設計上の問題**: `js/drive.js`の`deleteFile()`は`files.delete`を呼ぶ、
  **ゴミ箱を一切経由しない完全削除**だった。Google Driveの標準UIから削除したファイルなら
  ゴミ箱から復元できるが、APIで`files.delete`を呼ぶと最初からゴミ箱を経由しないため、
  ダイアログの誤操作と「完全削除でやり直し不可」という2つの問題が重なって、実機で本当に
  作品写真が失われる結果になった
- **対応(2026年9月)**:
  1. Extractの確認ダイアログを`showChoiceDialog()`に置き換えた(ラベル付きの対等なボタン、
     破壊的な方は`danger`オプションで赤系に強調する仕組みも`showChoiceDialog()`自体に追加した)
  2. **さらに踏み込んで、ユーザーから「アプリ側からDriveの元画像はいじらないで。バックアップ
     なので」という明確な方針が示されたため、「元画像をDrive上から削除する」という機能要件
     自体を撤去した**。Extractで「テクストのみ表示に切り替える」を選んでも、変わるのはこの
     カードの`mediaType`と`imageFileId`(このカードからの参照)だけで、Drive上のファイル
     本体には一切触れない(セッションのmediaフォルダに残り続け、Drive UI側からは引き続き
     見つけられる)
  3. `js/drive.js`の`deleteFile()`関数自体を削除した(呼び出し元が無くなったため)。もう1箇所
     あった同種の呼び出し(Mapping Storysの屋内地図スキャンを削除する`removeMapLayer()`、
     `js/modules/mapping-storys.js`)も同様に、Drive削除をやめてキャンバス上の参照を外すだけの
     動作に変更した
- **教訓**: 「A/B二択はshowChoiceDialog()を使う」という既存のルールは、新しい破壊的な機能を
  作るたびに適用箇所として思い出すこと。特に**元に戻せない操作(完全削除など)を伴う二択は、
  ダイアログの分かりやすさに加えて、そもそもその操作自体が本当に必要か(=アプリ側にDrive上の
  実体を削除する権限を持たせる必要が本当にあるか)を先に疑うこと**。今回は「機能を無くす」
  ことが最終的な解決策になった

---

## 今の技術構成(2026年8月時点で確定)

| 項目 | 内容 |
|---|---|
| ホスティング | GitHub Pages(静的サイト) |
| リポジトリ | https://github.com/Toshiyuki1988/constellation (Public) |
| 公開URL | https://Toshiyuki1988.github.io/constellation/ |
| キャンバス操作 | interact.js(ドラッグ・リサイズ・ピンチズーム) |
| データ保存 | Google Drive API を**ブラウザから直接**呼び出し(バックエンドサーバーなし) |
| Drive認証 | Google Identity Services のトークンフロー、`drive.file`スコープ(専用フォルダ方式、Drive UI上に見える) |
| OCR・AIガイド | Gemini API を**ブラウザから直接**呼び出し |
| GCPプロジェクト名 | CONSTELLATION(一度、OAuth同意画面の不具合で作り直し済み) |
| APIキーの扱い | `config.js`にはキーを直書きしない。**初回アクセス時にユーザー自身がダイアログへ入力し、ブラウザのlocalStorageにのみ保存**(リポジトリには一切含まれない) |
| 開発環境 | Windows PC + Windows Terminal + Node.js + Claude Code |

### Driveのフォルダ構成(2026年9月に整理)

```
Constellation/
├── constellation-data.json   … カード・セッションなどの構造データ(このフォルダ直下、メディアとは別置き)
└── media/
    └── <年セッション名>/
        └── <セッション名>/
            └── ...(セッションの入れ子の分だけ続く)/
                └── アップロードされた画像・動画・音声ファイル
```

- メディアと構造データ(JSON)を明確に分離し、かつメディアはアプリのセッション階層(年→展覧会→…)をそのままDrive上のフォルダ階層として再現する。ユーザーがDrive UI側から直接ブラウズ・整理しやすくするための設計(ユーザー要望)
- `js/app.js`の`resolveSessionMediaFolderId()`が、セッションの祖先を`breadcrumbPathTo()`でたどりながら、必要なフォルダを`js/drive.js`の`findOrCreateSubfolder()`で解決/作成する。一度作ったフォルダのIDは`session.driveMediaFolderId`としてセッション自身にキャッシュし、以降は同じフォルダを使い回す(Driveへの問い合わせを毎回行わない)
- フォルダ名はセッションの`name`(表示名)をそのまま使うため、**セッションのタイトルを編集すると、対応するDriveフォルダ名も追従してリネームされる**(`startSessionTitleEdit()`のcommit内、`renameDriveFolder()`、ベストエフォート)
- 2026年9月以前にアップロード済みだった既存のメディアファイルは、`js/app.js`の`migrateExistingMediaToFolders()`で新しい入れ子構造へ移行できる(ブラウザのコンソールから直接呼ぶ、`window.migrateExistingMediaToFolders`としてグローバル公開済み)。ワンタイムの移行処理なので、恒常的なUIボタンはあえて用意していない。`card.imageFileId`を持つ全カードについて、対応するセッションのフォルダへ`js/drive.js`の`moveFile()`(Drive APIの`addParents`/`removeParents`)で1件ずつ移動する

## なぜこの構成になったか(重要な経緯)

最初はGoogle Apps Script(GAS)でWebアプリとして作っていたが、以下の理由で**GAS自体を放棄**し、静的サイト+API直接呼び出しに全面移行した。

- **GASのHtmlServiceはサンドボックス化されたiframeで配信されるため、`getUserMedia`(ブラウザ内蔵カメラ)が使えない**。これはGoogle公式が「Apps Scriptの外でホストしてください」と明言している、修正不可能なプラットフォーム制約
- GASのページは別ドメインのiframeに入れ子表示されるため、相対URLでのリダイレクトが壊れるなど、iframe起因のバグに何度も苦しめられた
- `google.script.run`には送信量の実務上の上限があり、動画アップロードのために自前のチャンク分割ハックが必要だった
- これらは静的サイト+Drive API/Gemini API直接呼び出しの構成にすることで、構造的に解消される

## 判明している重要な制約・注意点

- **Google Photos API**:2025年3月の仕様変更で、既存ライブラリの読み取り・検索が不可能に(アップロードのみ可能)。2026年9月時点でも復活していないことを再確認済み。よって「Googleフォトから自動で写真を引っ張ってくる」設計は成立しない。写真の実体は常にこのアプリ経由でDriveへ
  - **要調査(2026年9月、次回以降のセッションで続きをやること)**: 代わりに「Google Photos Picker API」(`https://www.googleapis.com/auth/photospicker.mediaitems.readonly`)が新設されている。ユーザーがGoogleフォト側のピッカー画面で1枚ずつ手動選択する方式で、選択結果は`baseUrl`として返る。目的は「Driveへ二重に保存せず、Googleフォト側の実体をそのまま参照してストレージ節約したい」。**未解決の核心の疑問**: この`baseUrl`(または選択結果への参照)は、旧Library API同様に短時間(目安60分程度)で失効し、都度APIを呼び直して再取得する必要があるだけなのか(→メディアIDさえ保存しておけば長期参照可能で採用できる)、それともピッキングセッション自体に紐づいていて、後日の再アクセスには**ユーザーによる再ピック操作が必要**なのか(→「一度貼ったら半年後も見られる」という個人アーカイブの用途には向かない)。公式ドキュメントの表面的な記述だけでは判断できず、実際に実機のAPIキーで試すか、より詳細なリファレンス(sessions/mediaItemsのAPIリファレンス、特に`mediaItems.get`でセッション外から再取得できるか)を読み込む必要がある。ブラウザ完結(バックエンド不要)で呼べるかどうかも未検証
  - **実機確認済み(2026年9月)**: 上記のAPIとは別に、Googleフォトのウェブページ上の写真をブラウザで直接ドラッグし、このアプリのキャンバスへドロップする分には、Google側の画像URLへの`fetch()`がCORSでブロックされず成功することを実機で確認した(`js/app.js`の`handleViewportDrop()`/`extractDroppedImageUrl()`)。取得できた画像はGoogleフォトの元サイズに近い解像度だった(縮小サムネイルではなかった)。ただし正式なAPIではなくドラッグ&ドロップ時にブラウザが渡す`text/uri-list`/`text/html`のURLを流用しているだけの挙動なので、Google側の仕様変更で今後動かなくなる可能性はある
- **Gemini APIキーの形式変更**:2026年6月以降、旧来の`AIzaSy...`形式(Standard key)は廃止方向。Google AI Studio で新規発行すると`AQ.`から始まる新形式(Auth Key)になる。**この新形式には、現時点でHTTPリファラー制限などの制限機能が存在しない**(Cloud Console側にもAI Studio側にも見当たらなかった)。対策として、課金設定をしない/自動漏洩検知に任せる/APIキーをリポジトリに含めない(localStorage方式)、で運用している
- **Gemini API無料枠は「1日20リクエストのみ」(2026年9月、`gemini-3.6-flash`で実機確認済み)**:429エラーのレスポンス本文に`"quotaId": "GenerateRequestsPerDayPerProjectPerModel-FreeTier"`, `"quotaValue": "20"`と明記されていた。この20という枠は**プロジェクト+モデル単位で共有**されるため、インフォメーションカードの`parseExhibitionInfo()`とキャプションの`ocrImage()`など、Gemini呼び出し全てが同じ1日20回のプールを食い合う。枠切れ時は課金されずに429エラーになるだけなので絶対条件(完全無料運用)自体は守られる。日次リセットのタイミングは太平洋時間の日付境界が一般的。**対策として`GEMINI_MODEL`を`gemini-3.5-flash-lite`に変更した(2026年9月)**。無料枠は**1日250リクエスト**とユーザーが実機のAI Studioダッシュボードで確認済み(`gemini-3.6-flash`の20/日から大幅改善)。精度面は、OCRのような単純な書き写し(画像→テキスト)は`gemini-3.6-flash`と遜色ない一方、`parseExhibitionInfo()`のような込み入った文脈解釈・条件分岐を伴う構造化抽出タスクでは、休廊日パターンと矛盾/冗長な`exceptions`を生成するなど、Liteならではの精度低下が実機で複数回確認されている(詳細は下記インフォメーションカードのセクション)。ユーザーは「現地での文字入力の手間を省く」実利を優先し、**精度の甘さはコード側の後処理(`normalizeInfoParsedExceptions()`)とプロンプトの継続的な調整で補う**方針を明示している。今後もLiteモデル由来と見られる新しい解析ミスパターンが見つかったら、同様にプロンプトの具体化とコード側の正規化処理の両輪で対応すること(モデルを再度フル版に戻すことは、無料枠1日20という制約が現地OCR用途と相性が悪いため、ユーザーの意向により選択肢から外れている)
- **写真カードは自動でOCRしない(2026年9月に修正した過去のバグ)**:`js/app.js`の`handleImageSelected()`/`handleOpenCamera()`は、以前は写真をアップロード/撮影するたびに裏で`ocrImage()`を自動実行し、検出した文字をそのままメモ欄に書き込んでいた。作品の一部にたまたま文字(値札・注意書きなど)が写り込んでいるだけで意図しないメモが自動生成されてしまう不具合だったため削除した。**OCRは常にユーザーが明示的に選んだ時だけ**(📌ボタン/カメラの「OCR」モード)実行される、という設計を徹底すること。今後写真関連の機能を追加する際、うっかり同じ自動OCRを復活させないよう注意
  - ボトムツールバー/CONSTELLATION PIEの入口ボタンは2026年9月に「OCR」から**「テクスト」**へ改名した(`index.html`の`#tool-text`、`js/app.js`の`els.toolText`)。押すと`handleOpenTextTool()`が`window.confirm()`でOCR(カメラでキャプションを撮影して読み取る、従来通り)か直接入力(カメラを使わず空のテクストカードを作ってすぐフォーカスする)かを確認する二択になった。この確認ダイアログの実装パターンは、`handleCreateSession()`のセッション名入力(OCR読み取りか手入力か選ぶ既存のconfirm)をそのまま踏襲している。カメラ内部の「OCR」モード自体(`js/camera.js`の`mode: 'caption'`)は名称・実装とも変更していない
- **日本向けiPhoneはカメラのシャッター音を一切消せない**(サードパーティの「無音カメラアプリ」も含めて回避不可)。Live Photosモードならやや穏やかな電子音になるが、音声も一緒に録音される副作用がある
- **OAuth同意画面(Google Auth Platform)の「ブランディング」設定**:アプリ名・サポートメール・承認済みドメインなどの必須項目を全部埋めても、「アプリケーションのホームページ」「プライバシーポリシーへのリンク」のような**"\*"が付いていない一見任意の項目が未入力だと、構成が完了せず、テストユーザー追加が保存されない**という不具合(または仕様)にハマった。**この2つのリンク欄も埋めることで解決**。同じ症状(保存してもリロードで消える)が出たら、まずここを疑う
- GitHub Pagesは無料アカウントの場合、Publicリポジトリでないと使えない
- **キャンバスのズームアウト下限(`MIN_SCALE`、`js/canvas.js`)は元々0.2だったが、セッションが広くなる(カードが増える/Mapping Storysの地図が広範囲になる等)とこの下限で頭打ちになり、俯瞰ズーム(`fitAllCardsToScreen()`)で全体が画面に収まらないという実機報告(2026年9月)を受けて`0.01`まで下げた。** 完全に0にはしていない(`applyMapLayerTransform()`など`1/scale`で割る箇所が複数あり、0だとゼロ除算・NaNの原因になるため、実用上「無制限」とみなせる小さな値に留めている)。Mapping Storysの`PX_PER_METER=12`は旧`MIN_SCALE=0.2`に合わせて下げた値だったため、今回の変更でさらに引き上げる余地があるが、今回はユーザーから明示的な依頼が無かったため据え置いている
- **カード内のテキスト欄をドラッグ選択しようとするとキャンバスのパンと競合する不具合を修正(2026年9月)**: `js/canvas.js`の`interact(viewportEl).draggable({...})`(キャンバスのパン)は、既定では`ignoreFrom`指定が無く、カード上のどの要素から始まったポインタ操作も一律にパン候補として拾ってしまう構成だった。個々のカード自身の移動(`attachCardGestures()`)は`event.target.closest('button, textarea, input, a')`で除外済みだったが、interact.js側のパンはこの除外の恩恵を受けず、`.star-card-memo`(テキストカード等のメモ欄)を編集中に文字をドラッグ選択しようとすると、ブラウザ標準のテキスト選択とinteract.jsのパンが同時に発火して競合していた。個々の入力欄ごとに`pointerdown`で`stopPropagation()`を足す(Mapping Storys・サマリーカードの各種入力欄で採用してきた方式)のは漏れが出やすいため、`interact(viewportEl).draggable({ ignoreFrom: 'textarea, input, select' })`で一元的に解決した。今後キャンバス上(`canvas-content`配下)に新しい`<textarea>`/`<input>`/`<select>`を追加する場合、この設定のおかげでパンとの競合を個別に心配しなくてよい
- **編集ガイド表示中、PCのEキーでメモ欄を編集できる(2026年9月追加)**: 既存の「編集ガイド表示中はDeleteキーでも削除できる」という仕組み(`js/app.js`のグローバル`keydown`ハンドラ)と同じ場所に、Eキーでヘックスの「Edit」ボタンと同じ動作(メモ欄の表示・pointer-events有効化・フォーカス)を行う分岐を追加した。メモ欄を持たないカード種別では何も起きない
- **`index.html`の各`<script>`/`<link>`タグは`?v=YYYYMMDDx`形式のクエリでキャッシュバスティングしている。JS/CSSファイルを編集したら、そのファイルに対応するタグの`?v=`を毎回必ず新しい値に変更すること。** 2026年9月、Mapping Storysのモジュールファイルを3回連続で編集・pushしたのに`index.html`側の`?v=20260907a`を一度も更新し忘れ、ユーザーのブラウザがキャッシュされた古いJSを読み込み続けて「実装したはずの新機能(ボタンなど)が実機に出てこない」という報告に繋がった実例がある。同じファイルを1セッション中に複数回編集する時は特に忘れやすいので注意

## モジュール(必須ではない追加機能)の作り方

基本機能が一通り揃ったので、これ以降「あると便利」系の機能は「モジュール」と呼ぶ。**指示に単に「機能追加して」とだけあれば通常どおり基本機能として実装し、「【モジュール】として追加して」のように明示された時だけ、下記のモジュール専用のルールで実装する。** 個人利用アプリの規模で本格的なプラグインAPI(登録・有効/無効の動的切り替えなど)を作るのはオーバーエンジニアリングなので作らない。

### コーディング上の約束事
- 1モジュール = 1ファイル(`js/modules/<モジュール名>.js`)。`index.html` の `<script>` タグは他の基本機能より後ろに置く
- ファイル全体を `(function () { ... })();` で包み、トップレベルの変数・関数名を直接グローバルへ漏らさない。**これは2026年9月に `js/app.js` と `js/pie-menu.js` の両方で `const SVG_NS` を定義してしまい、アプリ全体が起動しなくなる事故があったための最低限のルール**(このプロジェクトは全JSファイルがグローバルスコープを共有する構成のため、トップレベルの名前が衝突すると読み込んだスクリプトごとエラーで死ぬ)
- 既存の `state` / `els` / カード描画関数などは今まで通り直接参照してよい。モジュールだからといって完全に独立させる必要はない
- 実装したら、下の一覧に1行追記する

### 見た目・操作の統一デザイン言語
「宇宙フィールドワーキングの拡張便利装備」のイメージで統一する。
- **グラスモーフィズム**:背景がぼけて透ける半透明パネル(`backdrop-filter: blur(...)`)で統一する
- **起動**:`js/module-launcher.js`(下記)経由のキーパッドコードで統一する
- **終了**:スワイプで左右に「飛ばして」消す、**に加えて必ず✕ボタンも用意する**(2026年9月追記)。PCのマウスドラッグはスワイプ判定(距離・速度・角度のしきい値)に乗りにくく、WormGateで「PCだと閉じにくい」という不具合報告があったため、スワイプはジェスチャーとして残しつつ、確実に閉じられる手段として画面右上に半透明の丸い✕ボタンを置く(WormGateの`.wg-close-btn`が実装例)。押した際はオーバーレイのスワイプ検出にpointerdownイベントが伝播しないよう`stopPropagation()`すること

### モジュールの起動基盤(`js/module-launcher.js`、コア機能)
モジューごとに専用の起動ジェスチャー(1本指で円を描く/2本指でひねる、等)を個別に発明する方式は、既存のパン/ピンチ/長押しメニューと衝突を繰り返した(2026年9月、WormGateで3回作り直した)。そのため、**起動ジェスチャーはこの1ファイルに一本化**した。

- キャンバス背景を2本指でダブルタップすると、1〜9のキーパッドHUDが開く
- 3桁のコードを入力すると、対応するモジュールが起動する(例: WormGateは`123`)
- モジュール側は `registerModuleCode('123', openXxx)` を1行呼ぶだけでよく、新しいジェスチャーを考える必要がない。これで理論上ほぼ無限にモジュールを追加できる
- PC(マウス)は2本指ダブルタップができないため、CONSTELLATION PIE(長押しメニュー、`js/app.js`)に「キーパッド」項目を用意し、`window.openModuleKeypad()`で同じキーパッドを開けるようにしてある。個々のモジュール専用のPIE項目は増やさない
- キーパッドの3x3配置は電話のキーパッドと同じ並びだが、中国古代の洛書(らくしょ)・魔方陣に見られる「9マスで秩序を表す」意匠を借り、中央(5)を特別な起点として扱い、マス同士を薄い線(主星・伴星を結ぶ線と同じ視覚言語)で結んでいる
- なお `js/pie-menu.js` には「2本以上の指が同時に触れると、指ごとに別々の長押しタイマーが並行して走りパイメニューが2つ重なって生成される」という潜在バグがあり、2本指ジェスチャーの試作時に頻発が判明したため合わせて修正済み(2本目の指が乗った時点で長押し判定を中断するようにした)

### 実装済みモジュール一覧
- **WormGate**(`js/modules/wormgate.js`、コード`123`):セッション内の写真をリングHUDで指ドラッグして探し、タップでキャンバス上の位置へジャンプする検索/ナビゲーション用モジュール。写真は`thumbDataUrl`を流用(フル画像は読み込まない)。`js/sound.js`にWormGate専用の効果音3つ(起動音・リング回転音・選択音)を追加した。
- **Crews**(`js/modules/crews.js`、コード`456`。電話キーパッドの中央の横一列であり、洛書の対角線(4+5+2ではなく4-5-6=15)でもある並び。中央5を軸に複数のペルソナが集う、というこのモジュールの性質にちなんで選んだ):文献・記録に登場する人物の言葉を借りて、サマリーカードをその人格の声で語らせるモジュール。導入のきっかけはグスタフソン&ハーポヤ『つぼみの本』だが、特定の一冊専用にはせず、あらゆる文献から人格(ペルソナ)を召喚できる汎用設計にした。
  - ペルソナの登録は【人物情報】【その言葉(引用)】の2項目だけ。性別・職業・口調のような構造化欄は持たず、口調・人となりは「その言葉」の引用そのものからGeminiに読み取らせる。表示名は【人物情報】の冒頭からクライアント側だけで自動生成する(`deriveName()`、API不使用)。長文になりやすいため、両欄には📷ボタンでその場でOCR読み取りできる(`js/camera.js`の`openCamera('caption')`を流用、複数回撮影すると改行で追記される)
  - データは`state.crews`(配列、`{id, personInfo, theirWords, name, avatar, enabled, createdAt}`)に持ち、既存のカード/セッションと同じ`constellation-data.json`へオートセーブされる(専用のDrive保存キーやバックエンドは作っていない)
  - **重要な設計判断: ペルソナの登録・保存時にGeminiは一切呼ばない。** 2026年9月、インフォメーションカードの展覧会リンク自動検索で`google_search`ツール(検索グラウンディング)を使ったところ、請求先アカウント非紐付けの無料キーでは割り当てがゼロで即座に`429 RESOURCE_EXHAUSTED`になることが実機で判明した(詳細は下記インフォメーションカードのセクション)。この教訓を踏まえ、Crewsでは「登録のたびに余分なAPI呼び出しを増やす」設計を避けている(一時的に「人物情報から名前・役割をGeminiに抽出させ、表示名を「名前：役割」の形にする」機能を試作したが、無料枠を無駄に使いたくないというユーザーの意向で撤回し、表示名はクライアント側の機械的な切り出しのみに戻した)。実際にGeminiが呼ばれるのは、ユーザーがサマリーカード上でそのペルソナのヘックスを押した瞬間だけ(=既存の`summarizeSession()`を1回呼ぶだけで、Education/Academicと全く同じ呼び出しパターン)
  - **人物情報・その言葉の閲覧(2026年9月追加)**: 表示名(ニックネーム)だけでは元の人物情報を思い出せない、という声を受け、サマリーカード上のペルソナのヘックスを**長押し**すると、`window.showCrewInfoPopup()`が【人物情報】【その言葉】の全文をコピーボタン付きで表示するポップアップを開く。**タップ(短押し)は従来通り生成、長押しは閲覧専用**でGeminiは一切呼ばない。長押し判定は`js/canvas.js`のカード編集ガイドと同じ`CARD_LONG_PRESS_MS`(300ms)を流用している
  - 統合ポイントは全て`js/app.js`側の小さなフック経由:`summaryCardInnerHtml()`が`window.crewsSummaryHexButtonsHtml()`を呼んでONのペルソナ数ぶんヘックスボタンを追加描画し、`wireSummaryCard()`がタップ/長押しを判定してそれぞれ`handleSummaryGenerate(card, el, crewId)`(`window.getCrewById()`で引数がペルソナIDかどうかを判定してから`summarizeSession()`へ`persona`を渡す)と`window.showCrewInfoPopup(crewId)`を呼び分ける。生成されたテクストカードには`card.crewPersonaId`/`crewPersonaName`/`crewPersonaAvatar`を持たせ、`renderCard()`が水色グラスモーフ(`.star-card--crew`)とペルソナの名前・絵文字ヘッダー(`.star-card-crew-head`)を付けて、通常のテクストカードと一目で区別できるようにしている
- **Mapping Storys**(`js/modules/mapping-storys.js`、コード`789`。電話キーパッドの下段。123=WormGate・456=Crewsに続き、洛書の3列を使い切る並びにちなむ):会場の地図(館内マップ、または現在地周辺の地形)を、建物・道路の輪郭だけ緑色の線としてキャンバスの最背面に敷くモジュール。GAS版では画像処理・地図APIの直接呼び出しがiframeサンドボックスの制約で難しく実現できていなかった機能。
  - **他のモジュールと違い、画面全体を覆う暗いオーバーレイではなく「小窓」(画面右上に浮かぶ、キャンバスを隠さない小さいパネル)にした。** 地図を配置している間もキャンバス(展開済みの地図やカード)が見えている必要があるため。終了はスワイプ(小窓の上部バーから)+✕ボタン、という他モジュールと同じデザイン言語は踏襲している
  - **屋内**: 紙の館内マップをカメラで撮影し、Canvas 2Dでグレースケール化→閾値二値化→白地を透明化(alpha 0)+線を緑色に、という画像処理をその場で行う(サーバー不要、GAS時代にできなかった処理)。ユーザー指示により文字(館名・部屋名)は一切残さない、輪郭だけの割り切った処理にしている。撮影には`js/camera.js`の固定モードを流用せず、このモジュール専用の最小構成カメラ(`getUserMedia`+シャッターボタンのみ)を自己完結で実装した
  - **屋外**: 地図タイル画像をスキャンするのではなく、**OpenStreetMapのOverpass API**から建物・道路の形状をベクターで直接取得する(APIキー不要・完全無料)。取得結果はそのまま緑の線分になるため画像処理・透明化は発生しない。GPS座標(現在地からの半径指定)をメートル→px換算係数(`PX_PER_METER`)でスケールする。**ユーザー指示により、建物は`name`タグを持つもの(=主要な建物名)だけを残すフィルタをかける**(全部の建物ではなく主要施設だけ)。道路にはこのフィルタをかけない(名前を持つ道路は少なく、輪郭の手がかりが消えてしまうため)
    - **`PX_PER_METER`は当初「写真カードの既定幅約240px ≒ 人物2人分[目安3.4m]」に合わせて70px/mにしていたが、実機で致命的な不具合が見つかり12px/mまで下げた(2026年9月)。** 70px/mだと半径150m(既定値)だけでキャンバス全体が約21000px四方になり、既存アプリのズーム下限(`MIN_SCALE=0.2`、`js/canvas.js`)でも画面に収まらず、展開しても移動ハンドルだけが見えて肝心の建物・道路は画面のはるか外、という不具合が実機報告から判明した。既存の「全カードが収まるまでズームアウト」機能(`fitAllCardsToScreen()`、`js/canvas.js`)も`MIN_SCALE`でクランプする設計になっており、アプリ全体でこの下限を尊重する方針のため、Mapping Storys側だけそれを無視するのではなく係数を下げて対応した。「写真カード基準の正確な実寸」より「展開したら必ず見える」ことを優先した判断。半径スライダーの上限も400m→200mに下げてある。地図の展開時には`fitViewportToMap()`が、既存の`fitAllCardsToScreen()`と同じ考え方(`MIN_SCALE`〜`MAX_SCALE`の範囲でズームフィット)で、地図全体が画面に収まるようキャンバスを自動でズーム・パンする
  - 屋内は正確な縮尺情報が無いため自動スケール推定ができず、見た目のデフォルトサイズで仮置きして手動リサイズに委ねる。屋外はGPS実距離から自動でスケールが決まる、という非対称な設計になっている
  - データは`session.mapLayers`(配列、各要素`{id, kind:'raster'|'vector'|'embed', ...}`)としてセッションに保存し、既存の`constellation-data.json`へオートセーブされる。屋内の画像はDriveのセッションmediaフォルダへ通常の写真と同じ経路(`resolveSessionMediaFolderId`+`uploadFile`)でアップロードする
  - **複数地図対応(2026年9月)**: 「広範囲を1枚の地図(特にGoogleマップ埋め込み)で覆うとズームが粗く、建物・道路が細かすぎて潰れる」というユーザー要望を受け、単一の`session.mapLayer`から配列の`session.mapLayers`へ変更し、1セッションに何枚でも地図(屋内・屋外ベクター・Google埋め込みを問わず、混在も可)を重ねて置けるようにした。旧データ(単一オブジェクトの`session.mapLayer`)は`getMapLayers()`が初回アクセス時に自動で1要素の配列へ移行する。各地図はカード同様、互いに独立したハンドル(移動・リサイズ・回転)を持つ。検索/現在地呼び出し・展開ボタンは、既存の地図を置き換えず常に新しい1枚として追加する方式にし(既存の「置き換えますか?」確認ダイアログは廃止)、続けて追加した時に真上へ重ならないよう既存の枚数に応じてカスケード配置する。ただし**カメラの自動フィット(`fitViewportToMap()`)はセッション最初の1枚目だけ**に限定した(2枚目以降も毎回フィットすると、並べて敷き詰めたい既存の地図がその都度画面外へ追いやられてしまうため)。Googleマップの地図種別・不透明度・グレースケールは地図ごとに独立した値を持つため、小窓の「配置済みの地図」欄に地図ごとの個別コントロール(行)として表示する設計にした(単一のグローバルなスライダーではない)
  - 統合ポイントは`js/app.js`の`renderAllCards()`に置いた1行のフック(`window.renderMappingStorysLayer()`)のみ。地図要素をカードより先にDOM挿入することで、z-index指定なしに自然と最背面へ潜らせている。地図は既定で`pointer-events:none`(カードのタッチ判定を一切邪魔しない)、この小窓を開いている間だけ`pointer-events:auto`になり、ドラッグで移動、ハンドルでリサイズ・回転できる
  - 展開(キャンバスへ反映)時には`js/sound.js`に追加した`playMappingStorysDeploySound()`(地面が広がるように包み込む、ゆっくり満ちていく持続音)と、地図が左から右へ広がるように現れるCSSアニメーション(`.ms-deploying`、`prefers-reduced-motion`に対応)を合わせて再生する
  - **「この土地の伝承」機能(2026年9月追加)**: Mapping Storysを「ただの地図」で終わらせず、地図と展覧会セッションの「あいだにあるもの」(地域性・Genius Loci)を探る機能にしたい、というユーザー方針から追加した。緯度経度を持つ地図(屋外ベクター・Googleマップ埋め込み。屋内スキャンは現実の座標を持たないため対象外)の「配置済みの地図」欄に「この土地の伝承を調べる」ボタンがあり、押すと`js/gemini.js`の`describeLocalLore(lat, lng, hint)`(検索グラウンディングなしのプレーンな呼び出し、屋外ベクターならOverpassで取れた主要建物名も手がかりとして渡す)が一段落の文章を返し、`layer.loreText`として保存する。**サマリーカードのASTR接続先収集(`collectConnectedImageParts()`)は据え置きにし**(ASTRをカード以外に繋げる拡張は今回は見送り)、代わりに`collectSessionTextContext()`側で`window.getMapLayers(session)`を呼んで`loreText`をそのまま文脈に混ぜ込む形にした。出典番号は振らない(地図はカードではなく`sources`配列へ積めないため)。屋外ベクター側は元々、取得後の座標をバウンディングボックスに正規化するだけで実際の緯度経度を持たない設計だったため、`fetchOutdoorMap()`/`deployToCanvas()`に緯度経度(`lat`/`lng`)をレイヤーへ持たせる変更も合わせて行った。何度でも再取得可能(上書き保存、キャッシュ・重複排除はしない)
    - **開発方針として明示された指針**: 「詩情的効果を狙う機能は、まず何でもいいのでテクストとして吐き出させることを優先し、凝った接続・UI演出は後回しにする」。今回のケースでは、ASTRの接続先モデルをカード以外にも広げる(=より「詩的」だが構造変更が大きい)案よりも、既存の`collectSessionTextContext()`にただ1行テキストを足すだけの案を優先したのがこの指針の実例
    - **建物単位の具体性が必要と判明(2026年9月)**: 緯度経度だけを`describeLocalLore()`に渡すと、検索グラウンディング無しのGeminiは市区町村レベルの一般論にしかならず「瀬戸市 深川公民館近くの廃小学校」のような建物単位の伝承・歴史を知りたいという要望に応えられなかった。対策として、地図ごとの伝承欄に手がかり(建物名など)の自由入力欄(`layer.loreHint`)を追加し、`describeLocalLore()`のプロンプトでそれを「調べる対象そのもの」として明示するようにした。**ユーザーが実機で比較した結果、同じ質問でも「サマリーカードの傾向欄にQ.として書いて聞く」より「伝承欄からGeminiへ直接一問一答で聞く」方が明確に精度が高いと確認された**。理由は、サマリーは`collectSessionTextContext()`でセッション全体の全カードのテキストを一つのプロンプトに混ぜ込むため文脈が薄まるのに対し、伝承欄の呼び出しは対象を1つに絞った単発プロンプトだから、と考えられる。**今後、Gemini呼び出しを伴う新機能を検討する際は、「セッション全体の文脈を混ぜ込むサマリー経由」と「対象を1つに絞った専用の直接呼び出し」のどちらが適切か、精度が重要な用途では後者を優先する形で使い分けを検討すること**
    - **自由質問モード(2026年9月追加)**: 手がかり欄のテンプレート方式(`describeLocalLore()`が固定の前置き・締めの文言を付加する)に加えて、ユーザーの入力文を一切加工せずそのまま`askGemini({ prompt: question })`へ渡す一問一答欄(`layer.loreQuestion`)も追加した。テンプレート方式・自由質問方式のどちらで取得しても出力先(`layer.loreText`)は共通で、最後に実行した方が表示・上書きされる。**精度を優先したい既知の限界**: 検索グラウンディングを使わない以上、Gemini自身が学習データに持っていない固有の事実(無名の廃校の詳細な歴史など)は、プロンプトをどう工夫しても「完璧」にはならない。自由質問モードは「テンプレートの余計な言い回しに埋もれさせない」という改善であり、Gemini自身が知らない情報を作り出させないための歯止め(「具体的な情報が乏しい場合は無理に創作せず」という指示)は`describeLocalLore()`側にのみ残っているため、自由質問モードを使う際はユーザー自身がプロンプト内に同様の歯止め文言を書き添えることが望ましい
    - **「Geminiで調べる(検索あり・新規タブ)」ボタン(2026年9月追加→同日中に廃止、下記参照)**: APIキー経由の無料枠は検索グラウンディングを有効化すると即429になる制約があるが(本ファイル上部の既存の注記を参照)、これとは別に、ユーザー自身のGoogleアカウントでログインする`gemini.google.com`の通常のWebチャット利用では検索グラウンディングが無料で使える。この既存の別チャネルを、Googleマップの呼び出しボタンと同じ「外部タブで調べて結果をコピペで持ち帰る」パターン(GAS放棄前のPrizmoGo OCRコピペ運用と同じ思想)で橋渡しした。押すと質問文をクリップボードにコピーしつつ`gemini.google.com/app`を新規タブで開き、ユーザーが得た回答は「貼り付け」欄に貼って保存すると`layer.loreText`に入る仕組みだった。
    - **手がかりテンプレート方式・自動コピー付き起動ボタンの廃止(2026年9月、同日中)**: 上記2つの方式について、ユーザー自身が実機比較した上で「これなら要らない」と判断し、同日中に撤去した。手がかりテンプレート方式(`describeLocalLore()`、`layer.loreHint`)は、自由質問モードで完全に代替できる(テンプレートより柔軟に、送信内容を自分で書ける)ため`describeLocalLore()`ごと`js/gemini.js`から削除した。「Geminiで調べる」の自動コピー+タブオープンボタンは、「タスクバー等から自分でGeminiを開くのとやることが変わらない」というユーザーの指摘通り付加価値が薄いため削除した。**ただし「貼り付け」欄(自分で得た回答をlayer.loreTextへ保存する手段)自体は、この機能でしかできないことなので残した。** 結果、現在の伝承欄は「自由質問(API直送)」+「貼り付け(手動で得た回答の保存)」の2本立てになっている。この一連のやり取りは、**新機能を足す時は安易に選択肢を増やさず、実際に比較して不要と分かったものは同じセッション内でもすぐ削る**という運用の実例として記録しておく
    - **「アクティブ地図」概念の導入(2026年9月、UI再設計)**: 複数の地図が重なっていると「地図種別・ズーム・不透明度・伝承などの操作がどの地図に対するものか」分かりにくいという実機報告を受け、これらの詳細操作を小窓の「配置済みの地図」リストから、**地図本体に直付けする編集パネル(`buildMapEditPanel()`)**へ丸ごと移設した。小窓側のリストは名前+削除ボタンだけの簡易一覧(`renderCurrentMapsList()`)に縮小した(削除は地図本体の✕ハンドルでも既にできるため、リストは「今何枚あるか」の把握用途が主)。
      - 表示は常に1枚だけ(`activeLayerId`、モジュール内のモジュールスコープ変数)。地図のいずれかのハンドル(移動/リサイズ/回転)を操作し終える(pointerup/cancel、ドラッグ中ではなく操作の**終わり**)と、その地図が自動的にアクティブになる。ドラッグ中に切り替えないのは、`wireEditHandlers()`が全レイヤー再構築(`renderMappingStorysLayer()`)でパネルの付け替えを行うため、ポインタキャプチャ中に対象のハンドル自身のDOMを消してしまう(=ドラッグが壊れる)事故を避けるため。既にアクティブな地図であれば再描画自体をスキップする(embedのiframeがsrc再読み込みで毎回リロードされてしまうのを防ぐため)
      - 新規に地図を呼び出した(`ensureEmbedLayer()`/`deployToCanvas()`)直後は、その地図が自動的にアクティブになる(タップし直す手間を省く)
      - パネルはCSSの`left:100%; margin-left:14px`で地図の右側に固定の実ピクセル幅(230px)で配置する(transformではなくlayout段階で位置が決まるため、地図のscale/rotationに影響されない)。JS側(`applyMapLayerTransform()`)は既存のハンドルと同じ「親のscale/rotationを打ち消す」処理だけをパネルにも適用している
      - 埋め込み専用の操作も伝承(緯度経度)も持たないraster(屋内スキャン)は、パネルの中身が空になるためそもそも作らない(削除は既存の✕ハンドルで足りる)
      - 既知の制約: パネルは地図の右側に固定オフセットで置くだけで、画面端からの距離を考慮した自動クランプは行っていない(既存の移動/リサイズハンドルも同様に画面外へ出うる設計のため、アプリ全体で許容している範囲の粗さに揃えた)
- **Flight Engineer**(`js/modules/flight-engineer.js`、コード`147`。電話キーパッドの左列。123=WormGate・456=Crews・789=Mapping Storysで洛書の3行を使い切っているため、左の3列目にあたるこの並びを割り当てた):矩形選択でカードをまとめて選び、新規セッションへ格納/その場で整列/セッションを解体できる一括編成モジュール。まず単体HTMLのモックアップ(`flight-engineer-mockup.html`、2026年9月)で操作感を検証してから本実装した。
  - **他の3モジュールと違い、キャンバス本体の既定ジェスチャー(背景ドラッグ=パン)そのものを起動中だけ置き換える初めてのモジュール。** そのため唯一の例外として、`js/canvas.js`(カード個別の長押し/移動、俯瞰ズームのダブルタップ判定)と`js/app.js`(セッションカードのタップで開く処理)に、`window.isFlightEngineerActive()`を見るだけの薄いガードを追加している。背景のパン自体は、interact.jsが要素をキーにInteractableを使い回す性質を利用し、`js/modules/flight-engineer.js`から直接`interact(els.viewport).draggable({ enabled })`を呼ぶだけで止め/戻しができる(`js/canvas.js`側の`viewportEl`と`els.viewport`は同一要素のため、新しいフック関数の追加は不要だった)
  - 起動中はヘッダー直下ではなくキャンバス右上寄りに常設の浮遊グラスモーフィズムバー(ON/OFFトグル・履歴)が出る。終了はスワイプ(バー上部の掴みバーから)+✕ボタンという他モジュールと同じデザイン言語を踏襲している。バーが閉じている間はFlight Engineerも自動的にOFFになる(コード`147`で毎回開き直す)
  - 矩形選択は交差判定(カードの一部でも矩形に触れていれば選択対象)。**モジュール起動中はセッションカードを1枚タップするだけでも単体選択になり、「解体」を含むコマンドパネルがすぐ出る**(矩形でわざわざ1枚だけ囲む必要はない、というユーザー要望)。ASTR接続の「またぐ」判定は見た目の交差ではなく、接続している2枚が操作後に別セッション所属になるかどうかで切る(格納で両方とも新セッションへ移る接続はsessionIdを付け替えて保持し、片方だけ移る接続だけを切断する)
  - 検討段階にあった「排出」(セッションの外へ出す)は、実装が複雑になる割に使用頻度が低いと判断し、ユーザー指示により見送った。セッションの中身を見るだけなら次項の「プレビュー」で、外へ出したければ「解体」(元に戻せる)で足りるという整理
  - **セッションカードをダブルクリックすると、パンくずを移動せず(=Flight Engineerのモードを抜けず)に中身を軽くプレビューできる**(読み取り専用のポップアップ、Crewsの人物情報ポップアップと同じ位置づけ。写真は`thumbDataUrl`を流用、それ以外はアイコン+ラベルで表示)
  - 一括移動は、確定後に残る選択枠そのものを掴んでドラッグする(カード個別のドラッグとは別動作として分離)。PC限定でShiftキーを押している間は、起動中でも背景ドラッグ=通常のパン、カードのドラッグ=そのカード単体の移動に一時的に戻る(タッチにShiftは無いためPCのみでよい)
  - **履歴を持つのは「格納」「解体」のみ**(整理・単発の移動は対象外)。**ユーザー要望により、アプリやPCのシャットダウンを挟んでも履歴から戻れるようにする必要があった**ため、モックアップ時点のクロージャ方式(ブラウザを開いている間だけ有効)をやめ、各エントリをカードID・座標・切断/保持したASTR接続の断片だけのプレーンなデータに設計し直した。当初はUndo/Redoの2本のボタン+別の履歴一覧、という構成にしたが、**機能が複数ボタンに分散して分かりにくい(実機で「履歴は見えるが戻れない」という不具合報告)**という指摘を受け、Undo/Redoボタンを廃止して**時系列の1本の履歴(`state.feHistory` + 現在位置`state.feHistoryIndex`)に一本化**、バーの「履歴」パネルの行を直接タップして任意の時点へジャンプする方式に作り直した(直近10件、超えた分は古い方から破棄、新しい操作をすると「先」の分岐は破棄という一般的な挙動)。既存の`cards`/`sessions`/`connections`と全く同じ`constellation-data.json`へオートセーブされる。ジャンプで格納/解体によって作られた/消されたセッションを表示中だった場合に備え、パンくずの無効な末尾を切り詰める`ensureBreadcrumbValid()`もあわせて追加した
  - コマンドパネル(格納/整理/解体)が画面下端近くのカードを選ぶと見切れて操作不能になる不具合(実機報告)を修正済み。パネルをDOMへ追加した直後に実際の高さを測り、画面下端に収まらない分だけ上へ押し上げている
  - **バーが消えて矩形選択モードから抜け出せなくなる不具合(2026年9月、実機報告)**: 起動中に何らかの拍子でバー(`.fe-bar`)が見えなくなり(再現条件・根本原因は未特定。DOMから外れた可能性を疑っているが確証は無い)、その間も矩形選択モード(`feActive`)がONのままで背景パンもできず、バーの✕ボタンにも触れられないため、リロードするまで永久に抜け出せない状態になった。原因を確定できなかったため、**症状を確実に解消する2段構えの保険**を入れた: (1)`openFlightEngineer()`が、バー要素が`els.viewport`から外れていたら(`!els.viewport.contains(feEls.bar)`)作り直さずそのまま挿し直す自己修復、(2)バーの状態に関係なく、`feActive`中は**Escapeキーで必ず`closeFlightEngineer()`が呼ばれる**非常口を追加。バーが今後も原因不明に消えることがあっても、Escapeで確実に抜けられる

なお、セッション・編集ガイド・Asterism(見た順の線・ASTR手動接続)・操作音・インフォメーションカード・サマリーカード(いずれも下記)などは基本機能そのもの(canvas.js/app.jsに直接組み込み済み)であり、上記のモジュール分離の対象ではない。

## サマリーカード(基本機能、2026年9月実装)

セッション全体をGeminiに読ませて要約させる、`mediaType: 'summary'`のカード。ボトムツールバーの「サマリー」ボタンで作成(CONSTELLATION PIEには追加しない、セッション/インフォと同じ扱い)。

- ヘッダーに、編集ガイドのヘックスバッジと同じ意匠(`clip-path`のヘックス形状)の特注ボタンを2つ持つ: **👦 Education**(小中学生にも分かるやさしい要約)と **🎓 Academic**(学術的な要約)
- カード本体に「要約の傾向」の自由入力欄があり、「フェミニズム的視点で」のような指示を書いておける(`card.summaryDirection`)
- ボタンを押すたびに`js/gemini.js`の`summarizeSession()`を1回呼び、結果は**新しいテクストカード**として生成される。同じボタンを何度押しても内容が変わって良い(キャッシュも重複排除もしない、押した回数だけ増える)
- 要約対象は`js/app.js`の`collectSessionTextContext()`が、サマリーカードのいる**セッション+入れ子の子セッション全て**を再帰的に辿って、各カードの`memo`(テキスト・キャプション・インフォの案内文など)を1つの文章にまとめたもの
- **既定では画像をGeminiに送らない**(セッションの写真枚数が多いと無料枠(1日250)をすぐ消費してしまうため)。ただし2026年9月の追加で、**サマリーカード自身のASTRヘックスから写真カードを手動接続すると、その写真(サムネイルのみ)も一緒にGeminiへ送って要約に反映する**ようにした(`collectConnectedImageParts()`)。動画・音声はデータ量が大きく無料枠をすぐ消費するため対象外、接続枚数も`SUMMARY_MAX_CONNECTED_IMAGES`(6枚)で上限を設けている。ユーザーが「セッション全体の文脈+特定の作品の見た目」を組み合わせて要約させたい、という発展的な使い方として追加された
- 生成されたテクストカードには`summarySourceId`(元のサマリーカードのID)を持たせ、`collectSessionTextContext()`から除外する(要約が過去の要約を再帰的に参照して内容が薄まっていくのを防ぐ)。サマリーカード自身(`mediaType: 'summary'`)も同様に除外する
- 生成→接続は既存のASTR手動接続の仕組み(`createAstrConnection()`)をそのまま流用しているだけで、専用の接続コードは書いていない。効果音(`playAstrConnectSound()`)・接続時の発光演出・オートセーブは全てそこに乗っている
- **出典の自動接続(2026年9月追加)**: `collectSessionTextContext()`は本文の各行に`[出典N]`タグを埋め込みつつ、`sources`配列(呼び出し側が渡す空配列、`card.id`をN番目に積む)を組み立てる。`js/gemini.js`の`summarizeSession()`はGeminiに本文(`answer`)と一緒に「最も参考にした出典番号」(`mostRelevantSource`)もJSONで答えさせ、`handleSummaryGenerate()`が`sources[mostRelevantSource-1]`から実際の`card.id`を引き当てて、出力テクストカードとその出典カードの間にも追加でASTR接続を張る(サマリーカード→出力の接続とは別に、もう1本)。特に参考にした出典が無ければ`mostRelevantSource`は`null`で、この追加接続はスキップされる
- 出力されたテクストカードの座標は、サマリーカードを基準にランダムオフセットで配置する(ユーザー指示: 「ランダムでOK」)
- **「Geminiの回答を貼り付け」欄(2026年9月追加)**: Mapping Storysの伝承欄と同じ思想・実装パターン(APIキー経由の無料枠は検索グラウンディングが429になるが、ユーザー自身のGoogleアカウントでログインする`gemini.google.com`の通常利用は検索グラウンディングが無料、という別チャネル)をサマリーカードにも導入した。当初は「要約の傾向」欄の文面をクリップボードへコピーしつつ`gemini.google.com`を新規タブで開くボタンも用意したが(セッション全体の文脈まで一緒にコピーする案もあったが、コピー量が増える割にGemini側は元々このアプリの記録を知らないため恩恵が薄いとして見送り、コピーは「要約の傾向」欄だけにした)、**「タスクバー等から自分でGeminiを開くのと変わらない」というユーザー自身の指摘を受け、同日中にボタンだけ撤去した**(伝承欄でも同じ理由で撤去済み、上記参照)。自分で得た回答を貼り付けて保存する導線だけが、この機能でしかできないこととして残っている。保存すると、Education/Academicボタンと同じ出力経路(`createTextCard()`+`summarySourceId`+`createAstrConnection()`)でテクストカード化される。`mostRelevantSource`による出典への追加ASTR接続は、人間が手で貼り付けた回答にはその情報が無いため対象外
- **「この質問をそのまま送る」ボタン(2026年9月追加)**: Mapping Storysの伝承欄の自由質問モードと同じ思想をサマリーカードにも導入した。Education/Academicが経由する`summarizeSession()`のテンプレート(JSON出力形式・文体指定・「前置きを書かないで」等の指示文)を一切通さず、`handleSummaryDirectQuestion()`が「要約の傾向」欄の文面をそのまま質問として`askGemini({ prompt, images })`へ直接渡す。**ユーザー要望により、`collectSessionTextContext()`(セッション全体の文脈)と`collectConnectedImageParts()`(ASTR接続画像)は、Education/Academicと同じく毎回まとめて添えて一括送信する**(「手動でGeminiチャットへコピペする場合もどのみち同じ情報を送ることになるので、非公開データの露出という点では変わらない。それより一括送信できる効率を優先したい」というユーザーの判断)。出力は`createTextCard()`+`summarySourceId`+`createAstrConnection()`でカード化される点はEducation/Academicと共通だが、`mostRelevantSource`による出典への追加ASTR接続は、テンプレートのJSON出力を経由しないため対象外。ボタンはEducation/Academic・自由記述の`.star-card-summary-input`欄・貼り付け欄と`summaryInFlight`(同時実行防止)・ボタンの無効化を共有する
  - **アステリズムフォーカス(2026年9月追加)**: サマリーカードから特定のカードへASTR接続がある場合、「前置き不要・その接続先を中心に」という短い指示だけを質問文の前に足すようにした(質問文自体は加工しない)。接続先がテキスト系カード(テキスト/インフォ/キャプション)なら、`collectSessionTextContext()`が既に本文へ埋め込んでいる`[出典N]`番号で名指しする(内容を二重に埋め込まず、Geminiにその番号の行へ注目させるだけ)。接続先が写真カードなら「添付した写真に写っている作品を中心に」という一文を足す(`summarizeSession()`の`hasImages`時のtaskInstructionと同じ言い回し)。接続が無ければ何も足さず、これまで通りの動作。「サマリー全体ではなく、ASTRで名指しした特定のカードだけについて聞きたい」というユーザー要望への対応
- 要約傾向の入力欄に質問文を入れても素直に答えてくれるため、指示文が入力されていれば出力テクストカードの冒頭に「Q. 〜」として残す(何を指示して出てきた要約か、後から見返しても分かるように)
- 編集ガイドはASTR(写真の手動接続用)とDeleteのみ(Depth・Edit・Captionは無し)。ドラッグでの移動と、カード本体の傾向入力欄への直接入力が主な操作手段
- 写真カードの編集ガイド「Extract」で「元画像を残す」を選んだ場合も、抽出テキストは同じカードのメモ欄に追記するのではなく、サマリーカードと同じ経路(`createTextCard()`+`createAstrConnection()`)で新規テクストカードとして出力する(長文になるとメモ欄がスクロール式になり読みにくいため、2026年9月に変更)
- **Extractの範囲選択(2026年9月追加)**: 「Extract」を押すと、OCRの前に`openExtractRegionPicker()`(`index.html`の`#extract-region-overlay`)がフルスクリーンのオーバーレイを開き、読み取りたい範囲を指/マウスでなぞって囲めるようにした。**四角形限定ではなく自由形状**(なぞった通りの多角形パス)で選べる。何も囲まずに「画像全体を読み取る」を押せば、これまで通り画像全体が対象になる。実装は、キャンバスの表示座標で取ったパスを画像の自然解像度(`naturalWidth`/`naturalHeight`)へ換算し、パスのバウンディングボックスサイズの一時canvasへ、パスをクリップパスにして元画像を描画する(選択範囲の外側は白地で塗ってからクリップする、透明のままだとOCR実装によっては黒として読まれることがあるため)。得られたcanvasを`toBlob()`でBlob化し、以降は`ocrImage()`へ渡す既存のフロー(検出結果の確認ダイアログ・破棄/残すの2択)へそのまま合流する。キャンバス上の小さいカード表示のまま範囲を選ばせると精度が出ないため、専用のフルスクリーンオーバーレイ(`js/camera.js`の常時黒背景オーバーレイと同じ見た目の思想)で元画像を大きく表示してから描かせる設計にした
- 写真カードの編集ガイドには「Summon」もある(`handleSummonSummary()`)。「サマリーカードを作る→ASTRで写真を手動接続する」という2手順を1手順にするショートカットで、押すとその写真に接続済みの状態のサマリーカードが新規作成される。写真限定(動画・音声カードに置いても、サマリー側は接続画像しか見ないため意味を持たない)
- インフォメーションカードと同様、Asterismの自動(見た順)線の対象からは除外する。**サマリーが生成したテクストカード(`summarySourceId`持ち)も同様に除外**しないと、連続生成した出力同士が作成時刻の近さだけで誤って自動接続されてしまう不具合があった(2026年9月に修正、`redrawAsterismLines()`)

### アイデアメモ(未着手、2026年9月にAsterism・モジュール・サマリーを組み合わせて出た思いつき)

ユーザー自身の評価: 「どっちも面白そうだが、展覧会の解釈そのものより星座という概念によりすぎている気もする。ただ予期せぬ方向で役立つかもしれない」という留保つき。実装の優先度は無く、あくまで記録。

1. **「星座図」モジュール**: 今のAsterismは線を引けても、カードの配置自体はユーザーが手でドラッグした座標に依存しており、「接続関係だけで見た星座としての形」を俯瞰する手段が無い。WormGateと同じ起動基盤(モジュール共通キーパッド)を使い、あるセッションのカード+ASTR接続を、実配置座標を無視して接続関係だけで放射状などに再配置・表示する専用モジュールを作る案。中心にサマリーカード、その周りに接続された写真・出典テキストが並ぶような、「今日どう読み解いたか」の形を可視化するビジュアライゼーション。
2. **AIによる星座の命名**: 本物の星座は星の並びそのものより「何を選んでどう名付けたか」に意味がある。あるセッションでASTR接続がある程度たまったら、Geminiに接続パターン(何と何が繋がっているか、サマリーの内容)を渡し、そのセッション固有の詩的な「星座名」を1つ提案させる小機能案(公式な星座名ではなく、内容にちなんだ独自の名前)。実用性より、記録に愛着を持たせるための遊び要素。

### 続き(2026年9月、Crews実装後の振り返りから)

Crewsモジュール(文献の登場人物の言葉で展覧会サマリーを語らせる)を実装した後、「このアプリの世界観・思想を反芻して、さらなるモジュールを考えてほしい」という問いかけに対して出てきた3案。ユーザーからは「以前提案した『星座寄り』のアイデアより、だいぶ理解を深めたね」という評価をもらった——上記1・2が星座という視覚的なモチーフそのものを面白がる方向だったのに対し、以下はアプリの本質(展覧会を何度も、違う声・違う時間軸で読み直す=反芻/多声性)から逆算して出てきた案だという違いがある。実装の優先度は無く、あくまで記録。

1. **木霊(こだま)— 過去の自分をペルソナとして召喚する**: Crewsは【人物情報】【その言葉】を文献などの他者から借りる仕組みだが、同じ構造は「半年前の自分」にも使える。過去のメモ・サマリー出力をそのまま【その言葉】として読み込ませれば、「あの時の自分は何を見ていたか」と対話できる。新しい仕組みはほぼ不要で、Crewsの召喚フォームに「過去の記録から選ぶ」入口を足すだけで実現できる案。時間を隔てた自分自身も「あらゆる立場」の一つ、という捉え方。
2. **巡礼路 — 特定の作家・テーマを横断的に辿る**: 上記「まだ実装できていない機能」にある「アーティスト目録(全セッション横断)」を、単なる索引ではなく「この作家に何度出会い、その度に何を書いたか」という時系列の物語として見せるモジュール案。「1回の鑑賞」ではなく「積み重ねた鑑賞史」そのものを反芻対象にする点でCrewsと地続き。
3. **声を交わす — Crewsのペルソナ同士を対話させる**: 今のCrewsは各ペルソナが独立に語るだけだが、2人以上ONのペルソナに互いの発言を読ませた上で応答させれば、実際に視点がぶつかる対話になる。「あらゆる立場から再考する」構造を、並置から本当の多声(ポリフォニー)へ一歩進める案。
4. **複数人での同時操作(ビデオチャットしながら一緒にキャンバスを触る)**: ユーザーからの雑談的な思いつき。「サーバーを持たない」という絶対条件と正面からぶつかる案で、映像・音声自体はWebRTCでピアツーピアにできても、接続相手を仲介するシグナリングだけはどうしても何らかのサーバー(Firebase無料枠等)が要る。さらに今`constellation-data.json`は1人のGoogleアカウントのDriveに丸ごと保存する設計なので、複数人が同時編集すると保存の競合(後勝ちで他人の変更を上書き)が起きる。個人用ツールとして育ててきた今のスコープからは大きく外れるため、本気で作るなら別の実験として切り出すべき、という結論込みで記録のみ。実装の優先度は無い。

## 前回の作業位置の復元(基本機能、2026年9月実装)

以前は`onSignedIn()`が読み込みのたびに`state.breadcrumb`を必ず現在の年セッションへリセットしており、アプリを再起動(再読み込み)すると常にその年のトップから始まっていた。ユーザー要望により、Driveに保存する`constellation-data.json`へ`breadcrumb`(パンくずのID配列)も含めるようにし、次回読み込み時にそこへ復元するようにした。

- 保存されたIDを先頭から順に`getSessionById()`で検証し、削除されたセッションを指していた箇所で打ち切る(Flight Engineerの`ensureBreadcrumbValid()`と同じ考え方の簡易版を`onSignedIn()`内にインライン実装)。1つも有効な箇所が無ければ、従来通り現在の年セッションへ戻す
- カードは生の座標(`scale:1`、原点`(0,0)`)のままだと画面外に散らばって見えるため、復元できた場合のみ既存の`fitAllCardsToScreen()`(俯瞰ズーム機能と同じもの)を呼んで自動的に画面へフィットさせる。**あえてピクセル単位の正確なパン・ズーム位置までは保存していない**(ウィンドウサイズがデバイスごとに違う中で厳密な座標を復元しても破綻しやすいため、「同じセッションへ戻ってきれいに見える」ことを優先した判断)
- `state.breadcrumb`はセッション移動のたび(`enterSession()`、パンくずクリック、`jumpToInfoCard()`)に`scheduleAutoSave()`を呼ぶよう変更した(以前はカード等のデータ変更時のみ保存していた)

## インフォメーションカード(基本機能、2026年9月実装)

展覧会の会期・開廊時間・休廊日を記録しておく、リマインダー用途のカード種別(`mediaType: 'info'`)。ボトムツールバーの「インフォ」ボタンで作成(CONSTELLATION PIEには追加しない)。

- 情報はテキストのコピペ(または手入力)のみで入力する。当初は展覧会公式ページのURL入力(Geminiの`url_context`ツールでの取得)を試したが、TOKYO ART BEATなど**クライアントサイドレンダリングのサイトでは本文が取得できず解析に失敗する**ため、ユーザーの判断でURL入力は廃止し、**ページ本文をコピペするテキスト欄のみ**に一本化した
- カード内の「解析する」ボタンを押した時だけ、`js/gemini.js` の `parseExhibitionInfo()` がGeminiに**一度だけ**問い合わせ、会期・時間・休廊日を構造化データ(`card.infoParsed`)として保存する。以降「今日は鑑賞可能か」は`isExhibitionVisitableOn()`でローカルJSのみで判定し、APIは再度呼ばない(完全無料運用の絶対条件と両立させるため)
- 祝日による休廊は、実行時に祝日カレンダーを持たずに済むよう、`closedWeekdays`(曜日パターン)には含めず、Geminiに**具体的な日付のexceptionとして個別に列挙**させる設計にした
- 解析に失敗した場合は`card.infoParseError`にエラーと読み取れた分(`partial`)を保持し、カード上に開始日・終了日など最小限の手動修正フォームを出す
- ヘッダーの`#infoTicker`に、**現在の年タブ内**で今日鑑賞可能な展覧会をローテーション表示する(階層をまたいだ全年横断はしない、というユーザーの指示による意図的なスコープ)。タップすると`jumpToInfoCard()`がそのカードのセッションまでパンくずを遡ってからジャンプする
- 編集ガイドは「開閉(展開/折りたたみ)」「Depth」「Delete」のみで、**ASTR(自動線・手動接続とも)は搭載しない**
- **本文コピペ欄は、一度解析済みになった後は✎「本文を編集」ボタン1つに格納される**(`card.infoRawEditing`、既定false)。解析前(または✎で編集中)は`<textarea class="star-card-info-text">`が展開されている。これは、テキストエリアのpointerdownを`stopPropagation()`している関係で、展開されたままだとカードのほぼ全面でピンチズームがキャンバスへ伝わらない不具合報告(2026年9月)を受けての対応。解析成功時(`handleInfoCardParse()`)・手動確定時(`handleInfoCardManualFix()`)には自動で`infoRawEditing = false`に戻し、再び格納する

`parseExhibitionInfo()`は現在ツールなしのプレーンなテキストプロンプトでGeminiを呼んでいる(URL廃止に伴い`google_search`/`url_context`ツールも削除済み)。

**2026年9月、検索グラウンディングは無料キーで即429になることを実機確認**: 一度、公式ページリンクの自動検索機能として`tools: [{ google_search: {} }]`(検索グラウンディング)を`parseExhibitionInfo()`に追加し、読み取ったtitle・venueを手がかりにGemini自身がGoogle検索を行い、`officialUrl`を同じJSON応答に含めさせる実装を試した。「日次のリクエスト上限は`generateContent`の呼び出し回数単位でカウントされるはずだから、ツールを1個追加しても呼び出し自体は1回のまま」という想定だったが、これは誤りだった。**実機で試したところ、インフォ解析(サマリー要約は同時に成功していたため、1日の呼び出し数上限とは無関係)が`429 RESOURCE_EXHAUSTED`で即座に失敗した**。エラー本文に明確な理由は無かったが、状況から「検索グラウンディング自体が、請求先アカウントを紐付けていない無料キーでは割り当てゼロで、有効化した時点で失敗する」という機能であると判断し、**完全無料運用の絶対条件と両立しないため撤回した**(`tools`指定を削除し、元のプレーンプロンプトに戻した)。今後、Gemini API呼び出しに`google_search`や類似の検索/ツール系オプションを追加する際は、まずこの制約を疑うこと。
代わりに、展覧会リンクは`js/app.js`の`exhibitionSearchUrl(parsed)`が、Geminiを一切呼ばずtitle・venueからGoogle検索結果ページのURL(`https://www.google.com/search?q=...`)をクライアントサイドだけで組み立てる方式にした。公式ページそのものを断定はできないが、タップすればユーザー自身がすぐ確認できる、無料枠を一切消費しない代替案。カード上に「Google検索で確認」リンクとして表示し、`syncInfoCardCalendar()`でカレンダーイベントの`description`にも同じURLを入れている。

## チャットカード「座談会」(基本機能、2026年9月実装)

サマリーカードのEducation/Academic(この機能の追加と同時に**表示名をBoy/Professorへ改名**。内部の`data-summary-mode`値・`summarizeSession()`のmode分岐('education'/'academic')は変更していない)・ONのCrewsペルソナ・そして人格を持たない素のGeminiが、1つの質問について一言ずつチャット形式で発言し合う、新しいカード種別(`mediaType: 'chat'`)。まず単体HTMLのモックアップ(`crews-roundtable-mockup.html`、2026年9月、git未管理)で操作感を検証してから本実装した。

- **起動**: サマリーカードに追加した「🗣 座談会をひらく」ボタン(`js/app.js`の`createChatCard()`)を押すと、「要約の傾向」欄の文面を最初の質問として、チャットカードがサマリーカードの隣にポップアップする(既存のテクスト出力カードと同じ「隣に配置」の作法)。傾向欄が空でも作成でき、その場合はチャットカード側から後で質問を投げる
- **参加者の固定スナップショット**: `buildRoundtableParticipants()`が、Boy・Professor・Gemini(素の人格、追加の文体指定なし)の3人に加えて、作成時点でONのCrewsペルソナ(`state.crews.filter(c => c.enabled)`)を`card.chatParticipants`として固定で持たせる。後からCrewsのON/OFFを切り替えても、既存の座談会カードの参加者構成は変わらない(会話の一貫性を優先した判断)
- **土台の仕組み(変わっていない)**: 参加者を1人呼び出す時、それまでの会話ログ全文(`card.chatMessages`)と`collectSessionTextContext()`(セッションの記録)を文脈として渡した上で、1〜2文の短い一言を生成させる(`fetchChatReply()`)。**「直前までの発言を全部見せた上で1人ずつ順番に呼ぶ」というだけの単純な仕組みで、特別な相互作用エンジンを実装せずとも自然に相互言及が起きる**(前の話者に賛成・反論・補足する、といった振る舞いは、渡している会話ログを読んだ上でGeminiが自発的に行う)
- **自動カスケード方式へ改修(2026年9月、実機フィールドワークからの要望)**: 以前は「▶ 次の発言を進める」ボタンでラウンドロビンを1人ずつ手動で進める設計だったが、「LINEで人間とやり取りしている感覚にしたい」という要望を受けて作り直した。質問コンポーザー(🎯宛先選択・📷写真添付・入力欄・送信ボタン)から質問を送ると、宛先(🎯ボタンから開く小窓/プルダウンで複数選択、未選択なら全員)が`runChatCascade()`によってランダムな順・ランダムな間隔(考え中0.5〜1.2秒→タイピングドット表示→0.9〜2.3秒後に発言)で自動的に1人ずつ返信する。発言の直前に`playChatReplySound()`(js/sound.js)で「シュコッ」という控えめな通知音を鳴らす。**API暴走防止のため、1回の送信につき各参加者は最大1回しか発言しない**(`chatTurnInFlight`で送信中は次の送信をブロックする)。旧`chatNextIndex`(ラウンドロビンの位置)は使わなくなったため削除した
- **質問はいつでも追加できる**: `card.chatMessages`には発言(`{kind, name, avatar, text}`)だけでなく、質問(`{type:'question', text, targetNames?, imageDataUrl?}`)も同じ配列に時系列で並ぶ。前回のカスケードが完了していれば、いつでも次の質問を送れる
- **写真添付(2026年9月追加)**: 質問コンポーザーの📷ボタンから1枚だけ画像を添付できる(`generateThumbnail()`でサムネイル化してconstellation-data.jsonにもそのまま保存、Driveへの別アップロードはしない)。添付があると、その回のカスケード中は宛先全員へ同じ画像が`askGemini()`の`images`として渡る(ASTR接続画像と同様、宛先ごとに絞る事はしない)
- **各参加者の文体指示**(`fetchChatReply()`内): Boyは「小学生・中学生にも分かるやさしい言葉で」、Professorは「学術的な視点から、専門用語を使っても構わない」、Crewsペルソナは`summarizeSession()`のペルソナ指示と全く同じ(【人物情報】【その言葉】から一人称の語りとして声を似せる)、Geminiだけは追加の文体指示を一切付けない素のプロンプト
- **無料運用の範囲内**: 検索グラウンディングは使わず(`askGemini({ prompt })`をテンプレート無しで直接呼ぶだけ)、1発言につきGemini API呼び出し1回。**宛先を絞らず全員に聞くと、その場で参加者数ぶんの呼び出しが連続して走る**ため、1日250回の共有プールを大きく消費したくない場合は🎯で宛先を絞る運用を想定している
- **Asterism・要約文脈からの除外**: 出力テクストカードと同様に`card.summarySourceId`(元のサマリーカードのid)を持たせ、既存の除外ロジック(`redrawAsterismLines()`の自動線、`collectSessionTextContext()`の再帰的な自己参照防止)にそのまま乗る。追加のコード変更は不要だった(`mediaType !== 'chat'`の明示的な除外も念のため`redrawAsterismLines()`に足した)
- **編集ガイド**: Edit(発言・質問のコピペ選択切り替え)とDeleteのみ(ASTR・Depth・Captionは無し)。移動・質問の送信・写真添付はすべてカード本体で完結する(本節末尾の「発言・質問のコピペ」を参照。以前このファイルには「Editは無し」という記載があったが、コピペ機能の追加当時から実際にはEditが存在しており誤りだったため訂正した)
- 会話ログは`.star-card-chat-log`(`flex:1`でカード本体の高さいっぱいまで伸縮、`overflow-y:auto`で収まりきらない時だけ内部スクロール)に収め、写真付きカードのメモ欄と同じ理由(キャンバスのズームにホイールを奪われないようにする)でホイールイベントをログ自身のスクロールに割り当てている。**カードをリサイズハンドルで広げると、会話ログもその分だけ大きく表示されて全体を俯瞰しやすくなる**(2026年9月要望。以前は`max-height:220px`で固定していたため、カードを広げても空白が増えるだけだった)
- **発言・質問のコピペ(2026年9月追加、同日中に方式変更あり)**: キャンバス全体は長押し編集ガイドのジェスチャーと競合しないよう既定で`user-select:none`だが、`.star-card-chat-text`/`.star-card-chat-question`にも`user-select:text`を追加し、発言・質問本文を選択してコピーできるようにした。**当初は常時選択可能にしていたが、`user-select:text`だけでは不十分だった**: `interact(viewportEl).draggable()`の`ignoreFrom`にこの2クラスを含め忘れており、ドラッグでの文字選択が引き続きキャンバスのパンと競合する不具合が実機で再発した。この修正と合わせて、ユーザーから「Editモードを設けて、その時だけドラッグ(選択)できるようにしてほしい」という要望があったため、**メモ欄と全く同じ仕組み(既定`pointer-events:none`→編集ガイドのEditボタン/Eキーで`.star-card-chat-editing`クラスをトグルして`pointer-events:auto`に切り替え)に作り直した**(テキスト自体は読み取り専用なので、実際に書き換えることはできない)。`ignoreFrom`にも`.star-card-chat-text, .star-card-chat-question`を追加した(pointer-events:noneの間はそもそもヒットしないため常時含めても無害)。`js/canvas.js`の`attachCardGestures()`(カード本体のドラッグ/長押し判定)にも、この2クラスからのポインタ操作を除外する分岐がある。編集ガイドが解除される(`deactivateEditGuide()`)と`star-card-chat-editing`もリセットされ、次に開いた時は毎回既定(選択不可)から始まる。**さらに、Editモードで文字を選択してCtrl+Cを押しても実際にはコピーできない不具合が実機で見つかった**: 既存のカードコピー機能(編集ガイド表示中にCtrl+Cでカードそのものをアプリ内クリップボードへコピーする、`document.addEventListener('keydown', ...)`)が`event.preventDefault()`でブラウザ標準のテキストコピーを奪ってしまっていたのが原因。`document.getSelection().isCollapsed`でテキスト選択の有無を見て、選択がある時はカードコピーを発動させずブラウザへ譲るようにして解決した

## コメントカード・コメント履歴・グループビューイング(基本機能、2026年9月実装)

Crewsたちと展覧会を「一緒に楽しむ」ための3機能。座談会(質問して答えてもらう、能動的)とは対照的に、こちらはCrewsが自発的に・受動的に反応してくる体験を狙っている。実装前に単体HTMLのモックアップ(`comment-card-mockup.html`、git未管理)で見た目を検証してからユーザーが承認、さらに生成のきっかけ・永続性についての追加指示を受けて本実装した。

- **3つの現れ方と、その永続性の違い(ユーザーが明確に指定)**:
  1. **カードコメント(自動・全セッション横断)**: 1日1回、`maybeAddRandomCardComment()`(`onSignedIn()`から呼ばれる)がアプリ全体の`state.cards`からランダムに1枚(`COMMENTABLE_MEDIA_TYPES = ['image','video','audio','text']`)選び、ONのCrewsペルソナからランダムに1人選んで一言コメントさせ、`mediaType:'comment'`の**普通のカードとして永続保存**し、ASTR接続する。**「僕の認識外で進んでほしい」というユーザー指示により、setStatus()等の通知は一切出さない**(成功も失敗もコンソール/デバッグログにのみ残す)。削除可能(既存のDeleteヘックスで足りる)
  2. **デイリーコメント**: `maybeShowDailyComment()`が`enterSession()`(セッション切り替え/年タブ切り替え)のたびと`onSignedIn()`(起動時)に判定し、**1日3回まで**、ONのCrewsペルソナ1人が現在のセッションについて一言をトースト表示する(`#daily-comment-toast`)。カードにはならない
  3. **グループビューイングモード**: ヘッダーの「👀 グループビューイング」ボタン(`toggleGroupViewing()`)で手動ON/OFF。展覧会で撮影を始めるタイミングで起動する想定。ONの間、`GROUP_VIEWING_INTERVAL_MS`(1分)ごとに、モード開始後に新しく取り込まれたカード(`createdAt >= groupViewingStartedAt`)からランダムに1枚選び、ランダムなペルソナに一言コメントさせて、デイリーコメントと同じトーストで表示する。新着が無いティックは何もしない(無理にコメントを作らない)。カードにはならない
  - **デイリー・グループビューイングは「コメント履歴」(`state.commentHistory`、最大`COMMENT_HISTORY_MAX`=100件、古い順に切り捨て)へ記録するだけの一過性データ、カードコメント(自動)だけが削除可能な永続カードとして残る**、という区別はユーザーが実装前に明確に指示したもの。ヘッダーの「💬 履歴」ボタン(`openCommentHistory()`)から、既存の`.modal-overlay`/`.modal`(`showChoiceDialog()`と同じ土台)を流用したモーダルで新しい順に閲覧できる
- **共通のプロンプト土台**: `personaVoiceInstruction(crew)`(【人物情報】【その言葉】から一人称の語りとして声を似せる、座談会・サマリーのCrews指示と同じ文言)と`fetchPersonaCommentOnCard(crew, targetCard)`(対象カードのmemo、画像カードならサムネイルも添えて一言だけ引き出す)の2つの共通関数を、手動の「💬 Comment」ヘックス・自動カードコメント・グループビューイングの3箇所すべてで使い回している
- **手動の「💬 Comment」編集ガイド(`handleCardComment()`)**: `COMMENTABLE_MEDIA_TYPES`のカードに付く。ONのCrewsペルソナが2人以上いれば`showChoiceDialog()`で誰に聞くか選ばせ(1人なら聞かずそのまま使う)、生成したコメントカードを対象カードへ`createAstrConnection()`(効果音・発光演出込み)で接続する
- **`createAstrConnectionSilent(cardIdA, cardIdB, sessionId)`という「静かな版」のASTR接続関数を新設した**: 既存の`createAstrConnection()`は`activeSessionId()`(今ユーザーが見ているセッション)をconnectionのsessionIdとして使う設計だった。手動操作(ユーザーが今見ているカードの編集ガイドを押す)なら常に一致するため問題なかったが、**自動のカードコメントは全セッション横断でランダムに選んだカード同士を繋ぐため、今アクティブなセッションとは限らない**。誤ったsessionIdで保存すると、その接続は`redrawAsterismLines()`のセッションフィルタに一致せず永久に描画されない静かなバグになるため、対象カード自身の実際の`sessionId`を明示的に渡す版を別に用意した。効果音・発光演出・ステータス表示も、この関数は一切行わない(自動生成の性質に合わせて)
- **`state.commentHistory`はconstellation-data.jsonへ通常通り永続化する**(`onSignedIn()`で読み込み、`handleSave()`で保存)。カードコメント(自動)の1日1回判定は`localStorage`の日付キー(`constellation-card-comment-date`)、デイリーコメントの1日3回判定も同様に`localStorage`(`constellation-daily-comment-progress`、`{date, count}`)で行う。**Gemini呼び出しが失敗した回はカウント/日付を戻し、次のセッション開閉・翌日起動時に再挑戦できるようにしている**(せっかくの1日ぶんの枠を通信エラー等で無駄に消費しないため)
- コメントカード自体(`mediaType:'comment'`)の編集ガイドはDeleteのみ。ASTRは持たない(生成時に既に接続済みで作られるため、自分から新しく繋ぐ運用は想定していない)。見た目はCrewsの水色(cyan)を踏襲した小さい読み取り専用カード(顔文字アバター・名前・コメント本文の3要素だけ)
- コメントカードは`redrawAsterismLines()`の見た順自動線・`collectSessionTextContext()`(ラベル「コメント」で通常のメモとして文脈に混ぜ込まれる、要約・座談会からの参照を妨げない)双方で、他のカードと同様に扱われる(自動線だけ除外、文脈収集は含める)

## Googleカレンダー同期(基本機能、2026年9月実装)

インフォメーションカードで解析した「鑑賞可能日」を、`展覧会`という名前の専用Googleカレンダー(`js/calendar.js`)に反映する機能。Drive APIと全く同じ構成(バックエンドなし、ブラウザから直接Calendar API v3を叩く)で実装した。

- OAuthスコープは`js/config.js`の`OAUTH_SCOPES`(旧`DRIVE_SCOPES`から改名)に`calendar.app.created`を追加した。`drive.file`と同じ「このアプリが作成したものにしかアクセスできない」最小権限スコープで、既存の他のカレンダーは一切見えない
- `ensureExhibitionCalendar()`が初回のみ`展覧会`カレンダーを新規作成し、そのIDを`state.exhibitionCalendarId`としてDriveの保存データ(`constellation-data.json`)にも永続化する(2回目以降は使い回す)
- **ガントチャートのイメージ**(ユーザー指定)を実現するため、会期全体を1本のイベントにはせず、`computeVisitableBlocks()`で休廊日を挟んだ「連続する開廊日のまとまり」ごとに分割し、ブロックごとに終日イベントを1つ作る(例: 定休日で週3日開廊なら週ごとに1本ずつ、のようにバーが飛び飛びになる)
- 同期は**差分更新ではなく、そのカードに紐づく既存イベントを全部消してから作り直す**単純な方式にした(`extendedProperties.private.constellationCardId`でイベントとカードを紐付けて検索・削除する)。個人利用の規模ではAPI呼び出し回数は問題にならないため、複雑な差分計算より単純さを優先した
- 同期タイミングは「解析する」ボタン成功時・手動修正確定時(`syncInfoCardCalendar()`)と、インフォカード削除時(`removeInfoCardCalendarEvents()`)のみ。**常時バックグラウンド同期ではない**(静的サイト+ブラウザ直呼びという構成上、アプリを開いていない間は何も起きない)
- カレンダー同期が失敗しても、インフォカード自体の解析結果(`card.infoParsed`)は失われないようにしてある(ベストエフォート、エラーはコンソールとステータス表示のみ)

### 要検証
Calendar APIの`calendar.app.created`スコープは、Google Cloud Consoleの「APIとサービス→ライブラリ」で**Google Calendar APIを有効化するだけ**で使える想定(Drive/Geminiのときと同様、OAuth同意画面へのスコープの個別追加は不要、というこのプロジェクトの既存の経験則に基づく)。実際にサインインし直した際、想定通り追加の同意画面(カレンダーへのアクセス許可)が出てスコープが通るかどうかは**実機で未検証**。

---

## これまでの設計変遷(要点のみ)

1. GAS単体アプリ→カメラのサンドボックス問題で挫折
2. 出先アプリ(カメラ専用)/帰宅後アプリ(編集専用)の2アプリ構成→iframeリダイレクトのバグで難航
3. iOS Shortcuts + Microsoft Pix経由でのOCR自動化を試みるも、Shortcutsのアクション仕様の不確実さ、位置情報トリガーの信頼性の低さ、Pixもシャッター音を消せないことが判明し断念
4. PrizmoGo(OCRアプリ)でコピー→アプリ内に直接ペーストする方式に簡略化
5. GAS自体を放棄し、GitHub Pages + interact.js + Drive/Gemini API直接呼び出しへ全面移行(今回の構成)

## 現場と自宅の役割分担(最終的な合意事項)

- **作品の写真・動画撮影**:アプリと切り離し、Pixや純正カメラで直接撮るだけ(通信もアプリの関与もゼロ)
- **キャプション・資料(伴星)**:現地でアプリを開き、OCRアプリ経由のコピペ、またはアプリ内カメラでの撮影→OCRで、その場でテキストとして打刻
- **帰宅後**:PCに取り込んだ写真・動画をアプリにドラッグ&ドロップ→EXIF時刻で近い伴星と自動ペアリング→「整列」で星座状に配置→AIガイド(展覧会名+伴星のテキストを元にWeb検索グラウンディングで概要・見どころ・比較を提示、感嘆符なしの淡々とした文体)

## 新規カードの生成位置(基本機能、2026年9月修正)

以前は、ボトムツールバー/CONSTELLATION PIEから作る全種類のカード(セッション・インフォ・サマリー・ストリートビュー・テクスト・アップロード/カメラ撮影/クリップボード貼り付け経由の写真・動画・音声)が、キャンバス座標の固定値`(40, 40)`に生成されていた。パン/ズームして別の場所を見ている状態でカードを追加すると、今見ている場所とは無関係な、離れた座標に生成されてしまうという実機報告を受けて修正した。

- `js/app.js`に`newCardSpawnPos()`を追加。`js/canvas.js`の`clientToContent()`(画面座標→キャンバス座標の変換、ドラッグ&ドロップの配置に既に使われていた関数)を使って、**今のビューポート中心が指しているキャンバス座標**を返す(連続で作った時に完全に重ならないよう、軽くランダムなずらしを加える)
- `handleCreateSession()`・`createInfoCard()`・`createStreetviewCard()`・`createTextCard()`・`createCardFromCapture()`(x/y省略時)・`createSummaryCard()`(x/y省略時)の既定位置を、この関数に差し替えた
- ドラッグ&ドロップ(`handleViewportDrop()`、`clientToContent(event.clientX, event.clientY)`でドロップ位置そのものを使う)や、サマリー出力・座談会カードなど「特定のカードの隣にポップアップする」設計のもの(`createSummaryCard(x, y)`に明示的な座標を渡す呼び出し)は元々正しく動いていたため変更していない

## パフォーマンス: 画像・動画のBlobキャッシュにLRU上限を追加(2026年9月)

「一日中PCで作業しても気づかなかったが、スマホ版が重くなっている。俯瞰でカードが多いとカクつく」という実機報告への対応。原因は2つに分けて考えている。

- **対応済み**: `js/app.js`の`blobUrlCache`(`getFileBlobUrlCached()`)は、開いた写真・動画の実データ(Blob、`URL.createObjectURL()`で作った参照)を「セッションの行き来で再描画されても再取得しない」という狙いで、これまで**上限無く保持し続けていた**。一日を通して複数のセッションを行き来していると、実質メモリリークになる。`BLOB_CACHE_MAX`(120)を超えたら、**現在のセッションのファイルは除外**しつつ、LRU(最も長く使われていないもの)から`URL.revokeObjectURL()`で実際に解放するようにした。現在のセッションを除外しているのは、表示中の画像・動画を誤って解放してしまわない(URLが失効して画像が消える事故を防ぐ)ため。「セッションの行き来で再取得しない」という既存の狙い自体は、直近120件ぶんという上限付きで維持される
- **対応済み(軽量版、ただし画像・動画・音声カードは対象外に修正)**: `renderAllCards()`は、現在のセッションに属する**カードを全て**(画面内に見えているかどうかに関わらず)フルDOM(編集ガイドのハンドル・ヘックス・メディア要素・メモ欄一式)として描画しており、**ビューポート外のカードを間引く仕組み(仮想化)は持たない**。「移動・ズームだけでもストレス無く行いたい」という要望を受け、DOMからカードを実際に着脱する本格的な仮想化(ドラッグ・リサイズ・ASTR接続・編集ガイドなど、カードのDOM生存を前提にしているコードが多数あり影響範囲が大きいため見送った)ではなく、`.star-card`へCSSの`content-visibility: auto`(+`contain-intrinsic-size`)を付けるだけの軽量な対策にした。DOM自体は変えずJS側の配線も一切変更していない。ブラウザが画面外と判断したカードだけレイアウト・ペイントを自動で省略し、視界に入れば通常通り描画される(対応ブラウザのみ効果、非対応ブラウザでは単に無視されるだけの安全なフォールバック)。
  - **実機で画質劣化の実機報告(2026年9月)**: ズームインした写真がいつまで経っても概観用の低解像度サムネイル(長辺240px・JPEG品質0.6)のまま、文字が全部ぼやけて読めない、という報告があった。当初はcontent-visibility:auto(画像・動画・音声カードの`observeMediaForLazyLoad()`が、他ならぬ`.star-card`要素自体にIntersectionObserverを仕掛けているため、同じ要素へcontent-visibility:autoを付けるとモバイルブラウザで相性問題が起きるのでは、という仮説)を疑い、`isPlainMediaCard`(画像・動画・音声カード)には`.star-card--media`クラスを付けて`.star-card:not(.star-card--media)`で対象から除外したが、**実機で直らなかった**(ズームの拡大・縮小操作を止めて安定させても劣化したままだったため、GPUラスタライズ由来の一時的なボケでもないと判明)。
  - **真因(2026年9月、判明・修正済み)**: `getFileBlobUrlCached()`が`fetchFileBlobUrl(fileId)`の返す**Promiseをそのままキャッシュし、失敗(reject)した場合もそのPromiseを永久に保持していた**。原因(OAuthアクセストークンの失効タイミングなど一時的なものが疑われる。`js/auth.js`の`ensureAccessToken()`はトークンが切れていれば`prompt:''`でのサイレント再取得を試みるが、モバイルブラウザ(特にiOS Safariのトラッキング防止機能)ではこのサイレント再取得が失敗しやすいと考えられる)で一度失敗すると、そのfileIdは**以後ずっと同じ失敗Promiseが返り続け、二度と再取得が試みられない**まま、`loadFullMedia()`側も`.then()`に`.catch()`が無く**失敗を静かに握りつぶしていた**ため、ユーザーからは「サムネイルのまま固定されている」ようにしか見えなかった。修正: (1) `getFileBlobUrlCached()`は失敗したPromiseをキャッシュから即座に削除し、次回呼び出しで再挑戦できるようにした。(2) `loadFullMedia()`に`.catch()`を追加し、失敗時は`console.warn`+`debugLog`(スマホでも🐞デバッグパネルから追える)で記録した上で、3秒待ってから`observeMediaForLazyLoad()`を呼び直し、画面内に入り直したタイミングで自然に再挑戦させる(`mediaLoadFailCount`で最大3回までに制限し、無限リトライは防ぐ)。**教訓**: Promiseをキャッシュする設計では、必ず失敗時にキャッシュから除去する処理を対で用意すること。無いと「一度失敗したら永久に直らない」という、原因調査が難しい類の不具合になる
  - **content-visibility:autoは後日撤回した(2026年9月)**。上記「画面外で何も描画されていない状態の内部だけの話であり、実際に画面内に入れば通常のレンダリングに戻る」という理解は**誤りだった**。spec上、content-visibility:autoは画面内表示に戻ってもcontain:layout style paint相当が外れず、paint containmentは常時効き続ける。このアプリでは編集ガイドのヘックスバッジ(`.star-card-hex`、カード枠から意図的にはみ出す配置)やサマリーカードのヘックスタブ(`.star-card-summary-head`、カード上辺をまたぐ配置)がまさにこの「常時のpaint containment」で常にクリップされてしまい、「サマリーカードの人物アイコンが上端で見切れる」「カード全体の表示がおかしい」という実機報告(2026年9月)につながった。ヘックスのはみ出しはCONSTELLATION全体の統一UI言語(編集ガイド)のため、カード種別ごとの除外(`.star-card--media`)では対処しきれず、`.star-card--media`クラス自体を含めcontent-visibility:autoの仕組みを完全に撤回した。**教訓**: content-visibility:autoやcontain系のCSSプロパティを导入する際は、「画面外の時だけ効く」という直感的理解を鵜呑みにせず、常時どの副作用(paint containmentによるクリップなど)が掛かるかをspec/実機の両方で確認すること。俯瞰時のカクつき対策は、下記の本画像フェッチの同時実行数制限のみで担う
  - **本画像フェッチの同時実行数を制限(2026年9月追加)**: 上記の修正後も「俯瞰からのズームインでカクつく、写真が劣化(低解像度サムネイルのまま)している」という実機報告が続いた。原因は、俯瞰(全体表示)でセッションに入った瞬間、画面内に見えている写真カードの数だけ`observeMediaForLazyLoad()`のIntersectionObserverが同時に発火し、`fetchFileBlobUrl()`がDriveから**元サイズファイルを無制限に並列フェッチ**していたこと。モバイル回線・メモリの圧迫で一部のフェッチが遅延/失敗し、`mediaLoadFailCount`のリトライ上限(3回)を使い切ると、そのカードだけサムネイルのまま永久に固定されてしまっていた。
    - **判断の基準にしたモデルアプリ(OneNote)の実機観察**: ユーザーがこの機能のモデルにしているOneNoteを実機で触ったところ、OneNoteでも「ズーム時に白く一瞬点滅する」「操作を止めてから0.5秒ほど経ってサムネイルが本画像に切り替わる」「パン時に何もないキャンバスへ写真がポップイン(呼び戻される)ように現れる」という、CONSTELLATIONと同種の遅延・ちらつきが起きることを確認した。この観察から、**「切り替えに一切の遅延・ちらつきを出さないこと」自体は目標にせず、「有限時間で必ず本画像に切り替わること」を目標に据え直した**(OneNoteは必ず約0.5秒で復帰するのに対し、CONSTELLATIONの不具合は復帰せず無限に固定される点が本質的な差だった)。この整理により、DOMからカードを着脱する本格的な仮想化(既存の`content-visibility:auto`除外の理由と同じく、ドラッグ・リサイズ・ASTR接続・編集ガイドなど「カードのDOM生存」を前提にしたコードへの影響範囲が大きい)には踏み込まず、より低コストな同時実行数の制限で対応する方針にした
    - `js/app.js`に`queueMediaFetch(fileId)`を追加し、`getFileBlobUrlCached()`が`fetchFileBlobUrl()`を直接呼ぶのをやめて、このキュー経由に変更した。`MEDIA_FETCH_CONCURRENCY`(4)を超える分は`mediaFetchQueue`で待機し、実行中のフェッチが1つ終わるたびに次を1つ取り出す。呼び出し側(`getFileBlobUrlCached()`)から見た挙動(Promiseを返す、キャッシュする)は変えていない
    - 優先度付け(画面中心に近いカードを優先する等)は行っていない、単純なFIFO。今回はまず「同時に殺到させない」ことだけを解決範囲とした
  - **本画像読み込みのトリガーを「画面内に入った瞬間」から「ズームしてフォーカスした段階」へ変更(2026年9月追加、ユーザー提案)**: 同時実行数を絞った後も、俯瞰(全体表示)で多数のカードが画面に入った瞬間、それら全部が読み込み待ち行列に並んでしまう(=実際にユーザーがズームインした1枚が後方に埋もれ、体感の切り替わりが遅い)という課題は残っていた。ユーザーから「サムネ→本画像への切り替えを、起動後の時間経過ではなく、人間の視覚がその写真にフォーカスしたと判断できるズーム状況で行えないか」という提案があり、この方針で実装した。
    - `js/canvas.js`の`applyViewportTransform()`末尾に`scheduleZoomSettleNotify()`を追加。ズームスケールが変化した(純粋なパンでは発火しない)後、`ZOOM_SETTLE_MS`(450ms、OneNote実機観察の「操作が止まってから0.5秒ほど」に倣った値)だけ操作が止まったら、`js/app.js`の`onViewportScaleSettled()`を1回だけ呼ぶ、というデバウンス。ピンチ/ホイールズームの途中で毎フレーム発火させない
    - `js/app.js`側は、`generateThumbnail()`のサムネイル長辺(240px、`FULL_RES_TRIGGER_PX`)を「これ以上大きく表示するとサムネイルの画素が足りずぼやける」しきい値として流用し、カードの`.star-card-media`が画面上で実際にこのpx以上の大きさで表示されているかどうかで本画像読み込みの可否を判定する(`isMediaDisplaySizeEnoughForFullRes()`)。`observeMediaForLazyLoad()`のIntersectionObserverは、画面内に入っても、このサイズ条件を満たすまでは`unobserve`せず監視を継続する(=サムネイルのまま据え置く)。ズーム操作中はカードが画面内に留まったまま(交差状態そのものは変化しない)ためIntersectionObserverは再発火しない。この「画面内だが未読込」なカード要素の集合を`pendingMediaElements`(Set)として持ち、上記の`onViewportScaleSettled()`から走査し直して、しきい値を超えたものだけをその場で本画像に切り替える
    - 既にズームインした状態でパンして新しい写真が視界に入ってくる場合は、IntersectionObserverの入場イベントの中でサイズ条件を即座に満たすため、450msの遅延なしにそのまま読み込まれる(遅延が入るのはズーム操作そのものが解決に必要な場合のみ)
    - `renderAllCards()`(セッション切り替え時に`els.content.innerHTML = ''`で全カードDOMを破棄する)の先頭で`pendingMediaElements.clear()`も追加した。Set が破棄された旧DOM要素への参照を強参照のまま持ち続けるとGCされずリークするため
    - 既存の「救済策としてのショートカットジェスチャー(明示的に本画像を呼び出す操作)」案も検討したが、ズーム連動の自動切り替えが体感の主目的(スムーズさ)を満たせる限りは不要と判断し、今回は見送った。もし実機でこの方式でも「ズームしたのに切り替わらない」体感が再発するようなら、次はショートカットジェスチャーの追加を検討する
  - **画質劣化の本当の原因はbackground-imageだった: `<img>`要素への置き換えで解決(2026年9月、実機確認済み)**: 「本画像フェッチの同時実行数を制限」「ズームでフォーカスした段階で読み込む」という上記2つの対策を経てもなお、実機では「本画像に切り替わる白点滅は確認できるが、切り替わった先が『荒いピクセル感のサムネイル』→『なめらかだが小さい文字はぼやける中間品質』の2段階止まりで、Driveで直接開いた時の(文字までくっきり判別できる)本来の解像度には到達しない」という報告が続いていた。**つまり2026年9月に修正した一連の不具合(失敗Promiseの永久キャッシュ、content-visibility:auto、フェッチの同時実行数)はいずれも「本画像が正しいタイミングで取得できるか」の問題であり、実際の元凶は全く別のレイヤー、取得した本画像データを画面にどう描画するかという表示側にあった**。
    - **原因**: 写真カードは`mediaEl.style.backgroundImage = url(...)`で表示しており、このカードは`.canvas-content`(`transform: scale(...)`でズームを表現する祖先要素)の子孫にあたる。**CSS `background-image`は、祖先が`transform`でスケールされる構成だと、モバイル端末のGPU合成レイヤーのラスタ解像度が実際のズーム倍率まで追従せず頭打ちになる(=ズームを続けても一定以上シャープにならない)**という、iOS Safari/モバイルChromeでよく報告される既知のレンダリング上の制約だった。ダウンロードした画像データそのものは(Drive上のファイル自体が無劣化であることをユーザーが実機確認済み、`js/drive.js`の`uploadFile()`/`fetchFileBlobUrl()`もリサイズ処理を挟まず生バイト列をそのまま扱う設計であることから)常に正しく、劣化は100%描画側で起きていた。動画・音声は元々`<video>`/`<audio>`要素で表示しており今回の問題が最初から起きていなかったことも、この原因説明と整合していた
    - **対策**: 写真の表示を`background-image`から、動画・音声と同じく実体を持つ`<img>`要素(`js/app.js`のサムネイル表示・本画像表示の両方、`css/style.css`に`.star-card-media img { object-fit: contain; width/height:100%; display:block }`を追加)に置き換えた。既存の`background-size: contain`と同じ見た目(縦横比を保ったまま全体を表示、はみ出さない)を`object-fit: contain`で踏襲している
    - **教訓**: CSSの`transform: scale()`で表現するパン・ズームキャンバスの内部に写真を敷く際、`background-image`はズーム時の実解像度追従に既知の制約があるため避け、`<img>`(または`<video>`と同様のネイティブ要素)を使うこと。同様の「ズームしても一定以上シャープにならない」症状が今後別のカード種別(ストリートビューのパノラマ`.star-card-media-streetview`、Mapping Storysの地図レイヤー等)で出た場合も、まずbackground-image/CSS背景描画の使用箇所を疑うこと

## メモ欄のハイパーリンク(基本機能、2026年9月追加)

セッション・写真/動画/音声・テクスト・ストリートビューの各カードが共通で持つメモ欄(`.star-card-memo`)に、URLをクリックできるリンクとして表示する機能を追加した。

- **表示の二重構造化**: 以前はメモ欄そのもの(`<textarea>`)が表示も編集も兼ねており、`<textarea>`は平文しか描画できずURLをクリックさせられなかった。`memoFieldHtml(card, hasMemo)`が、既定で見えている読み取り専用の`.star-card-memo-view`(`linkifyMemoHtml()`でURL部分だけ`<a target="_blank">`に変換したHTML)と、Edit中だけ表示される入力専用の`.star-card-memo`(`<textarea>`、実データはこちらが保持)のペアを返すようにした。編集ガイドのEditボタン/Eキーを押すとビュー→textareaへ、blurするとtextarea→ビュー(直前の入力内容を反映)へ切り替わる
- **`linkifyMemoHtml(text)`**: `https?://`で始まる文字列を検出し、それ以外の部分は`escapeHtml()`でエスケープしてから`<a>`タグへ差し替える(XSS対策、hrefも同様にエスケープ)。文末の句読点・閉じ括弧はURLの一部として誤って拾わないよう切り離す
- **リンクは既定でもクリックできる**: `.star-card-memo-view`自体は他のメモ欄と同じくpointer-events:noneのまま(カードの移動を優先)だが、`<a>`要素にだけ`pointer-events:auto`を個別に付けている。`js/canvas.js`の`attachCardGestures()`が元々`<a>`をカードのドラッグ/長押し判定から除外済み(公式ページリンク等のため)だったので、Editモードのような追加のゲート無しに、既定表示のままリンクだけ安全にクリックできる(単純なクリックはドラッグ距離が無いため、interact.jsのパンとも競合しない)
- メモ本文自体のコピペ(テキスト選択)は、従来通りEditモードで表示されるtextarea経由で行う(この変更による退行は無い)
- **リンク先のファビコン(2026年9月追加)**: 「検索先のサムネも取得」という要望への対応。任意のURLのOGP画像を取得するにはCORSの制約で外部サイトのHTMLをブラウザから直接scrapeできず、Geminiの`url_context`/`google_search`系ツールは無料キーで429になることが既に判明している(本ファイル上部の既存の注記を参照)ため、記事内容が伝わる本格的なサムネイルではなく、**APIキー不要・無料枠を消費しないGoogleの公開ファビコンサービス(`https://www.google.com/s2/favicons?domain=...`)**で妥協した。`linkifyMemoHtml()`が検出した各URLの`hostname`をこのURLへ渡し、`<a>`タグの中に小さい`<img>`として埋め込む(クリックはリンク全体で受け付けるため、アイコン単体にpointer-eventsの追加設定は不要)。`new URL(url)`でホスト名を取り出せない(=正規のURLとして解釈できない)場合は、ファビコンを省いてリンクだけ表示する

## OCRのバックグラウンド実行・PiP表示(基本機能、2026年9月追加)

実際に展覧会場でアプリを使ってみたユーザーから「OCR(Geminiへの問い合わせ、数秒かかることがある)を待たされている間、次の写真をすぐ撮りたい。サクサク写真とキャプションを取り込みたい」という要望があり、`js/camera.js`のテクストモード(キャプション読み取り)を全面的に非ブロッキング化した。

- **以前の挙動**: 「この範囲を読み取る」/「全体を読み取る」を押すと、カメラのフルスクリーンオーバーレイを開いたまま`ocrImage()`(Gemini呼び出し)の完了を待ち、その間ずっと次の撮影ができなかった
- **変更後**: `handleSelectionRun()`は画像の切り出し(ローカル処理、一瞬)だけを待ち、その直後に`detachCameraForBackgroundOcr()`でカメラのオーバーレイを即座に閉じる。OCR自体(`ocrImage()`)は`runOcrInBackground()`が裏で進め、進行中であることは画面右下の小さなPiP表示(`#ocr-pip`、`css/style.css`)だけで示す。カメラは即座に次の撮影に使える状態へ戻り、その間アプリの他の操作も一切妨げない
- **呼び出し元のロジックは変更不要だった**: `openCamera('caption')`は元々`Promise<CaptureResult>`を返す設計で、呼び出し元(`js/app.js`の`handleOpenCamera()`・`handleCardCaption()`・`handleCreateSession()`・セッション名編集のOCRボタン、`js/modules/crews.js`の`ocrIntoTextarea()`)はこれを`await`しているだけだった。**resolveを呼ぶタイミングを後ろへずらすだけで、結果の反映先(新規テクストカード/カードのメモへ追記/セッション名/入力欄/Crewsのテキストエリア)ごとに違うルーティングロジックには一切手を入れずに済んだ**。これは「Promiseを返す非同期関数」という既存の設計のおかげで、内部実装(いつ・どこで解決するか)を差し替えても呼び出し側に影響しない好例
- **結果到着時のDOM消失に対する安全策(2026年9月)**: OCRがバックグラウンドで数秒〜かかるようになったことで、結果が届く前にユーザーが別のセッションへ移動したり、対象のカード/入力欄/パネルを閉じたりできるようになった(以前はカメラのオーバーレイが画面を占有していたため起こり得なかった新しいリスク)。`handleCardCaption()`は`cardElById()`/`getCardById()`で結果到着時に最新の要素・カードを取り直し、カード自体が消えていれば新規テクストカードとして文字を残す。セッション名編集のOCRボタン、Crewsの`ocrIntoTextarea()`も同様に`input.isConnected`/`textareaEl.isConnected`を確認し、対象が既に無ければ新規テクストカードへフォールバックする(読み取った文字を握りつぶさない)
- 旧実装にあった、撮影直後に静止フレーム上へ文字プレビューを表示する「hold chip」(`.cam-hold-chip`)と、オーバーレイ内の不定進捗スライドバー(`.cam-ocr-progress`)は、どちらもオーバーレイを開いたまま待つ設計を前提にしていたため完全に削除した(index.html・css/camera.css・js/camera.js)
- サウンドは追加していない(YAGNI。PiP表示とステータス欄の文言だけで足りると判断した。今後「気づきにくい」という実機報告が出たら検討する)

## Wi-Fi接続時のみDriveへアップロード(基本機能、2026年9月追加)

「モバイル通信を使っている場合はアップロードを使用不可にして、Wi-Fiポイントに来た時に一括アップロードにしてほしい。通信量節約のため」というユーザー要望を受けて実装した。**ユーザー自身、「ドライブバックアップ思想と逆行するが、通信量節約が優先」と明言している**——「アプリ側からDrive上の元画像を削除・変更する機能は一切作らない」という絶対条件(本ファイル上部参照)とは矛盾しない。あくまで「いつアップロードするか」というタイミングだけの制御であり、アップロードした後のファイルには一切触れない。

- **新規ファイル`js/upload-queue.js`**(コア機能、モジュールではないため`(function(){...})()`では包んでいない。`js/drive.js`の直後に読み込む)
- **通信種別の判定**: `navigator.connection`(Network Information API)はAndroid Chrome系では概ね使えるが、**iOS Safariには2026年9月時点で実装が無い**。使える端末では`initUploadNetworkDetection()`が`change`イベントを購読して自動追従する。使えない端末(iPhoneなど、このプロジェクトの主要な利用環境)では、ヘッダーの`#upload-network-btn`(📶 Wi-Fi / 📵 モバイル(保留))をユーザー自身がタップして手動切り替えする運用にフォールバックする。**初回・判定不能時の既定値は「保留」**(通信量節約を優先するユーザー方針を、判定できない場合でも安全側に倒すため)
- **保留中のBlobはIndexedDBへ永続化する**(`constellation-upload-queue`データベース、`cardId`をキーにした`pending`ストア)。メモリ上だけに置くと、Wi-Fiへ辿り着く前にタブが再読み込みされたり、iOS Safariがバックグラウンドタブのプロセスを終了させたりした場合に、その日撮った写真がまるごと失われてしまうため。IndexedDBはBlobをネイティブにstructured cloneできるので、シリアライズの手間は不要
- `createCardFromCapture()`(file-input・カメラ・クリップボード貼り付けの全経路が通る共通処理)が、カード生成時に`isUploadAllowedNow()`を見て分岐する。モバイル中なら`persistToUploadQueue()`でIndexedDBへ保存するだけに留め(`card.uploadQueued = true`)、Wi-Fi中なら従来通り`uploadCardFileInBackground()`をその場で呼ぶ。IndexedDB自体が使えない環境等で保存に失敗した場合は、**通信量節約よりデータを失わないことを優先し、その場でアップロードする**フォールバックを入れてある
- **Wi-Fiになった瞬間(自動検知 or 手動トグル)に`drainUploadQueue()`が待機列を少しずつ(`UPLOAD_QUEUE_CONCURRENCY=2`)アップロードしていく**。既存の`uploadCardFileInBackground()`をそのまま使い回している(キューを経由するかどうかで挙動を変えていない)
- **アプリ再読み込み時の復元**: `onSignedIn()`から`restoreUploadQueueOnLoad()`を呼び、前回終了時にキューへ残っていたぶんを拾い直す。`card.uploadQueued`が立っているのに対応するIndexedDBの実データが見つからない(別端末で作られた・ブラウザのストレージが消去された等)場合は、詰まったままにせず「アップロード失敗」扱いにしてユーザーが気づけるようにする
- **UI**: 待機中のカードは`.star-card--upload-queued`(青系の破線・「📵 Wi-Fi待ち」バッジ)で、既存の「アップロード中」(`--upload-pending`)・「アップロード失敗」(`--upload-failed`)とは視覚的に区別している。ヘッダーのボタンには待機件数もバッジ表示する(`updateUploadNetworkButton()`、js/app.js)
- **既知の制約**: IndexedDBは端末(ブラウザプロファイル)ローカルのため、PCとスマホなど複数端末を併用していると、片方でキューに積んだファイルはもう片方からはアップロードできない(その端末で改めてWi-Fiに繋いでアプリを開く必要がある)。個人利用・単一主端末を前提にした設計として許容している

## まだ実装できていない機能(新構成側)

- **次に着手予定(2026年9月、ユーザー指示)**: 「Crewsたちと一緒に展覧会を楽しむ機能」。現地でのOCR/アップロード周りの改善(上記2件)の次にやることとして明示されている。詳細未設計、着手時にユーザーと相談すること
- 主星・伴星のデータモデル自体(GAS版では実装したが、新構成ではこれから)
- 伴星の「アプリ内で直接打刻」機能
- EXIF時刻での自動ペアリング・写真取込
- 「整列」(星座状の自動配置)ロジック
- AIガイド(Web検索グラウンディング)
- アーティスト目録(Google Sheets連携、全セッション横断)
- 帰宅後アプリ側でのキャンバス編集全般(GAS版で作った機能の移植)

## 現在の状態(このメモを書いた時点)

- OAuthサインイン・Drive接続まで動作確認済み
- interact.jsによるキャンバス操作の実装中
- ヘッダー・ボトムツールバーのUI調整中

---

## 引き継ぎ後、最初にやること

新しいチャット/Claude Codeのセッションでは、まずこのファイルを読んだ上で、

1. 現在のリポジトリ(https://github.com/Toshiyuki1988/constellation )の中身を確認
2. 直近で何を作業していたか(ヘッダー/ボトムツールバーの幅調整)の続きから再開
3. 上記「まだ実装できていない機能」から、優先順位を相談して着手

という流れで進めるとスムーズです。
